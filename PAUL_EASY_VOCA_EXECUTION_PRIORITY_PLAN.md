# PAUL_EASY_VOCA_EXECUTION_PRIORITY_PLAN.md — 복귀 후 첫 실행 계획

_작성: 2026-07-26. **순수 설계 문서 — 코드/SQL을 이 세션에서 전혀
작성·실행하지 않았다.** 이 대화의 전체 문서(9개)를 종합한 **최종
실행 순서**다. `PAUL_EASY_VOCA_MVP_ROADMAP.md`가 "학원 수 기준
4단계"로 조직했다면, 이 문서는 **"지금 당장 무엇부터 타이핑을
시작하는가"**를 P0/P1/P2 + 30/60/90일 캘린더로 조직한다 — 관점만
다를 뿐 결론(순서)은 그 문서와 완전히 일치해야 하고, 실제로 일치한다._

---

## 1. 현재 상태 평가

`PAUL_EASY_VOCA_CURRENT_STATUS.md` 요약: 111명 실사용, production-ready
판정 완료. **문제는 "무엇을 새로 만들 것인가"가 아니라 "이미 완성된
것을 왜 아직 안 썼는가"다** — Critical 보안수정, 쓰기 AI 보조 v1.3,
게임화 5종 전부 **코드 완료·운영자 액션(SQL 실행/배포) 대기** 상태로
쌓여 있다. 이 사실 하나가 아래 P0 전체의 근거다.

---

## 2. P0 — 반드시 먼저 해야 하는 것

| 항목 | 예상 효과 | 예상 난이도 | 위험도 | 먼저 해야 하는 이유 |
|---|---|---|---|---|
| **v3_11 커리큘럼 보안수정 배포** | 인증 없이 커리큘럼 전체 CRUD 가능한 Critical 취약점 완전 차단 | 낮음(`docs/audit/2026-07-26-v3_11-1hour-runbook.md` 60분 체크리스트 존재) | 낮음(RLS 추가만, DML 없음, 즉시 롤백 가능) | 라이브에 열려 있는 유일한 Critical 보안 이슈 — 다른 무엇보다 절대적으로 우선 |
| **GitHub Pages 완전 차단** | 그림자 배포 재발 가능성 완전 제거 | 매우 낮음(Settings 1건) | 없음 | 5분이면 끝나는 일을 미룰 이유가 없음, 이미 원인은 규명됨 |
| **쓰기 AI 보조 실운영화**(Edge Function 배포+SQL 2건+flag ON) | 이미 완성된 자산이 실제 가치를 내기 시작(교사 검토 시간 절감) | 낮음(코드 완료, 배포 순서만 남음) | 낮음(미리보기+수동승인 구조, 자동반영 없음) | 완성된 코드가 배포만 안 돼서 가치를 못 내는 것 자체가 이 시점 가장 큰 낭비 |
| **게임화 SQL 일괄 실행**(v2.5~v2.8) | Paul Rank/House/Ticket/Word King/Season이 학생 화면에 실제로 나타남 | 낮음(SQL 실행뿐, 코드는 이미 배포됨) | 낮음(전부 opt-in 기본 false, 반별로 켤 때만 노출) | 매몰 자산 회수 — 이미 몇 주치 개발이 끝나 있음 |
| **관리자 PIN 형식/강도 검증** | 최소 보안 위생 확보 | 매우 낮음 | 낮음 | 외부 학원(P1 이후)을 받기 전 최소 조건 |

**공통점**: P0는 전부 **"새 코드를 짜는 게 아니라 이미 있는 것을
실행하는 것"**이다 — 이게 왜 이 시점 최우선인지의 핵심.

---

## 3. P1 — 중요한 개선

| 항목 | 예상 효과 | 예상 난이도 | 위험도 | 먼저 해야 하는 이유 |
|---|---|---|---|---|
| **`academy_id` 멀티테넌트 스키마 실제 착수** | 외부 학원을 받을 수 있는 기술적 전제조건 확보 | 중간(설계는 `SAAS_ARCHITECTURE_PLAN.md` §4에서 이미 끝남 — 6개 anchor 테이블만 필요, 실제 마이그레이션 작성·백필만 남음) | 중간(기존 학원 데이터에 영향 가능 — academy #1 백필 원칙, 멱등 설계 필수) | `MVP_ROADMAP.md` Phase 2 진입의 유일한 게이트, 그러나 지금 학원이 여러 곳은 아니므로 P0급 긴급은 아님 |
| **Vercel 유료 플랜 전환** | Hobby ToS(비상업 한정) 리스크 완전 제거 | 낮음(설정+결제) | 낮음 | 외부 학원을 받는 순간 필수 — 미리 해두면 급하게 전환할 일이 없음 |
| **Memory Engine(SRS) 최소 구현** | 학습 효과 자체의 핵심 개선(다학원 확장과 무관하게 독립적 가치) | 중간(6-box Leitner 설계는 이미 끝남, `MASTER_PLAN.md` §1) | 낮음(신규 테이블 추가뿐, 기존 로직 안 건드림) | 이미 "최고 ROI" 판정을 받은 항목(`MASTER_PLAN.md`) — 다학원 여부와 무관하게 지금 착수 가치 있음 |
| **무필터 전체조회 스코핑 설계 착수** | 다학원 확장 시 확정적으로 터질 성능 병목의 선제 대응 | 높음(15+ 호출부) | 중간(광범위한 코드 변경) | 지금 111명에선 안 급하지만, `academy_id` 작업과 자연스럽게 같은 시기에 설계해두는 것이 효율적(같은 파일들을 두 번 건드리지 않기 위해) |
| **AI 배치 채점 N+1 수정** | 비용/성능 개선(기존 감사에서 유일하게 남은 실질 최적화) | 낮음(batch select/upsert) | 낮음(fire-and-forget 경로) | 저위험·고효율, 미룰 이유가 딱히 없음 |

---

## 4. P2 — 미래 기능

| 항목 | 예상 효과 | 예상 난이도 | 위험도 | 먼저 해야 하는 이유(=아직 안 하는 이유) |
|---|---|---|---|---|
| **Teacher 역할(다중 관리자)** | 중형 학원(교사 2인+) 지원 가능 | 중간 | 중간(인증 모델 변경) | `MVP_ROADMAP.md` Phase 3(20곳)부터 필요 — 지금은 원장 1인 학원뿐이라 시기상조 |
| **결제 시스템**(subscriptions/billing) | 자동화된 매출 관리 | 중간 | 낮음 | Phase 3부터 — 고객이 아직 5곳도 안 되는데 결제 자동화부터 만드는 건 순서 착오 |
| **Platform Admin Dashboard** | 다학원 운영 가시성 | 중간 | 낮음 | Phase 3~4부터 — 지금은 관리할 학원이 1곳뿐이라 존재 이유가 없음 |
| **AI Teacher(문장 첨삭 등 신규 AI 기능)** | 신규 학습 가치 | 높음(신규 AI 워크플로우) | 중간(아동 안전 고려 필요, `AI_LEARNING_ENGINE_PLAN.md` §6.3) | 기존 쓰기 AI 보조(P0)조차 아직 실배포 안 된 상태에서 새 AI 기능부터 설계하는 건 우선순위 착오 |
| **위험학생/이탈 예측(규칙기반)** | 교사 개입 지원 | 낮음~중간(규칙기반이라 기술적으로는 쉬움) | 낮음(단, 윤리적 사용 원칙 준수 필수, `LEARNING_ANALYTICS_PLAN.md`) | `academy_id`도 아직 없는 상태에서 예측 모델은 의미 없음 — 데이터 스코프가 먼저 정리돼야 |
| **Enterprise 물리적 격리 인프라** | 대형/프랜차이즈 고객 유치 가능 | 높음 | 낮음(선택적 옵션이라 실패해도 영향 적음) | 그 규모 고객이 아직 존재하지 않음 — 지금 만들 이유가 전혀 없음 |

---

## 첫 30일 계획

**목표**: P0 전부 완료 + P1 착수.

| 주 | 내용 |
|---|---|
| Week 1 | P0 전부 실행 — v3_11 배포(런북 그대로), GitHub Pages 차단, 쓰기 AI 보조 배포, 게임화 SQL 실행, 관리자 PIN 강도 검증. **전부 "코드 완료, 실행만 하면 되는 것"이라 1주 내 완료 가능** |
| Week 2 | P0 완료 검증(각 항목 실제 동작 확인) + Vercel 유료 플랜 전환 |
| Week 3~4 | `academy_id` 마이그레이션 SQL 작성 착수(설계는 끝났으므로 Database Agent가 실제 SQL 파일 작성, 실행은 아직) + Memory Engine SRS 스키마(`word_review_schedule`) 설계 확정 |

---

## 60일 계획

**목표**: 멀티테넌트 인프라 실제 적용 + Memory Engine 최소 구현.

- `academy_id` 6개 anchor 테이블 마이그레이션 실행 + 기존 학원
  academy #1 백필 + RLS 적용(`SAAS_ARCHITECTURE_PLAN.md` §4)
- 무필터 전체조회 스코핑 착수(`academy_id` 작업과 병행 — 같은 파일들을
  건드리는 작업이라 이 시기에 함께)
- Memory Engine SRS 최소 구현(6-box Leitner 로직 + Garden 연동)
- AI 배치 채점 N+1 수정

---

## 90일 계획

**목표**: `MVP_ROADMAP.md` Phase 1 완료 확정 + Phase 2(첫 외부 학원)
착수 준비.

- 학원별 관리자 인증(전역 `ADMIN_PIN` 폐지) 적용
- 데이터 격리 실측 검증(`academy_id` 필터 하나로 완전 분리되는지)
- Interleaving(신규/복습 혼합) 구현
- **첫 파일럿 학원 1~3곳 실제 접촉/온보딩 준비**(`PAUL_EASY_VOCA_
  CUSTOMER_OPERATION_PLAN.md`의 §1~2 가입/초기세팅 프로세스를 여기서
  처음 실전 사용)
- 이 시점부터 `MVP_ROADMAP.md` Phase 2가 공식적으로 시작

---

## 지금 절대 만들면 안 되는 기능 TOP 10

`MVP_ROADMAP.md`의 TOP10(가챠/리더보드/스트릭페널티 등 **영구 금지**
원칙)과 겹치지 않게, 이 문서는 **"이 90일 안에 순서를 착각해서
먼저 손대면 안 되는 것"**에 초점을 맞춘다.

1. **`academy_id` 전체 구현을 P0보다 먼저 착수** — v3_11도 안 끝난
   상태에서 스키마 확장부터 하면 우선순위가 완전히 뒤집힌다.
2. **결제 시스템을 지금 만들기** — 고객이 5곳도 안 되는데 자동 결제
   인프라부터 짜는 건 시간 낭비(§4 P2 근거 그대로).
3. **Platform Admin Dashboard를 지금 만들기** — 관리할 학원이 1곳뿐인
   지금은 존재 이유가 없다.
4. **Teacher 역할/다중 관리자를 지금 만들기** — 원장 1인 운영 학원뿐인
   지금 시기상조.
5. **AI Teacher(문장 첨삭 등) 신규 AI 기능부터 설계** — 기존 쓰기 AI
   보조(P0)조차 아직 배포 전인데 새 AI부터 손대는 건 우선순위 착오.
6. **위험학생/이탈 예측 모델을 지금 구현** — `academy_id`도 없는
   상태에서 예측 스코프 자체가 정의되지 않는다.
7. **확률형 보상/가챠 시스템**(`MVP_ROADMAP.md` TOP10과 동일, 영구 금지)
8. **학생 간 공개 순위·리더보드**(동일, 영구 금지)
9. **신규 과목 확장(수학 등)** — 지금은 완전히 시기상조, 미션 재심사
   조차 필요 없는 단계(그 논의 자체가 너무 이름).
10. **Enterprise 물리적 격리 인프라** — 그 규모 고객이 존재하지 않는
    지금 만들 이유가 전혀 없다.

**1~6, 9~10은 "나중에 순서가 되면 만든다"(§4 P2)는 것이지 "영원히
안 만든다"가 아니다** — 7·8만 이 앱의 원칙상 영구 금지다. 이 구분을
헷갈리지 않는 것이 이 문서의 핵심 메시지.

---

## 관련 문서

`PAUL_EASY_VOCA_CURRENT_STATUS.md`, `PAUL_EASY_VOCA_MASTER_PLAN.md`,
`PAUL_EASY_VOCA_SAAS_ARCHITECTURE_PLAN.md`, `PAUL_EASY_VOCA_AI_
LEARNING_ENGINE_PLAN.md`, `PAUL_EASY_VOCA_MVP_ROADMAP.md`(동일 결론의
학원수 기준 조직), `PAUL_EASY_VOCA_CUSTOMER_OPERATION_PLAN.md`,
`docs/audit/2026-07-26-v3_11-1hour-runbook.md`(P0 1번의 실행 절차
원본).
