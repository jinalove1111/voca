node_modules
dist
.env

.vercel
.env.vercel*.local

.env*

scripts/.tmp

# 개발자 대시보드 산출물(scripts/generateDashboard.mjs가 매번 재생성) — 생성 스크립트만 커밋
dashboard/

# Supabase CLI 로컬 링크 상태(supabase link가 생성, 프로젝트 ref 포함 — 커밋 불필요)
supabase/.temp/

# 운영 산출물(학생 실명·UUID 포함 — 절대 커밋 금지, 2026-08-08 보안 점검)
# ops/ 아래 CSV/SQL에는 실학생 이름과 student_id가 들어 있다. 이 저장소는
# 공개(public)이므로 실수로 add되면 개인정보가 그대로 노출된다.
ops/

# 로스터 정리 마이그레이션 SQL(2026-08-09, v3_24~v3_28)에는 학생 실명이
# 주석에 다수 포함돼 있어 공개 저장소에 커밋 금지 — 운영자 로컬에서만 실행.
# (v3_23은 Paul 테스트계정만 포함하나 일관성·정책상 함께 추적 해제.
#  단 과거 커밋 3032b8b에는 남아 있음 — 완전 제거는 history rewrite 필요.)
supabase_v3_23_paul_dedup_rename.sql
supabase_v3_24_roster_canonical_class_moves.sql
supabase_v3_25_roster_archive_empty_dups.sql
supabase_v3_26_roster_sca_prestep.sql
supabase_v3_27_roster_dedup_active_rename.sql
supabase_v3_28_roster_container_dup_rename.sql
# 실명이 든 로스터 정리 체크포인트(위 SQL과 동일 사유)
.ai-status/roster-cleanup-sql-2026-08-09.json
.ai-status/roster-cleanup-v328-2026-08-09.json
