# 블로커 (Blockers)

> 지금 진행을 막고 있는 항목만 기록한다. append-only 원칙 —
> 해소되면 항목을 지우지 않고 "해소됨(일시, 방법)"으로 표시를 바꾼다.
> `DECISIONS_PENDING.md`(운영자 결정 대기)와는 다르다 — 이 파일은
> "지금 당장 막혀서 못 하는 것", `DECISIONS_PENDING.md`는 "결정만
> 내리면 진행 가능한 것"을 구분해 기록한다.

## 현재 활성 블로커

**없음 (2026-09-25 기준).** Paul Town 2.5D 프로토타입(179차 세션) 작업
진행에 현재 막힌 항목이 없다.

## 기록용 — 활성 블로커 아님

- **V2 S16 항목17 자석 드래그 간헐 실패(179차, 2026-09-25)**: `npm run
  verify:e2e` run 2(HEAD `01450a0d`)에서 `[town-v2] S16[390x844,mouse]
  자석 드래그 배치 항목17`이 FAIL했다. 근거: 동일 코드가 run
  1(`b5430e9e`)에서는 PASS했고, 179차 세션은 Paul Town **V2**
  코드(`paulTown2_5d`와 완전히 독립된 별도 실험)를 전혀 수정하지
  않았다 — `TESTING.md`의 기존 V2 드래그 측정 노트에도 유사 간헐성이
  이미 기록돼 있다. **이 세션의 진행을 막지 않았다** — S14/S15/
  build/verify:all/Vercel Preview까지 전부 정상 완료됐고, 179차의
  파일럿 준비 범위(`paulTown2_5d`)에는 영향이 없다. V2 재현/분리
  조사는 별도 세션 몫으로
  `DECISIONS_PENDING.md`("V2 S16 항목17 자석 드래그 간헐 실패")에
  남겨뒀다. 상세는 `handoff.md` 2026-09-25(179차) §6.1 참고.

## 표준 제약(블로커가 될 수 있는 조건, 상시 기록)

아래는 이 저장소/세션에서 구조적으로 지켜야 하는 제약이며, 이를
어기지 않고서는 진행할 수 없는 작업이 있을 경우 그 시점에 "현재 활성
블로커"로 승격해 기록한다.

- **Production DB WRITE 금지(에이전트 세션 한정)**: 에이전트/CI는
  Supabase에 DDL을 직접 실행할 수 없다(`CLAUDE.md` 규칙 8). 새
  테이블/컬럼이 필요한 작업은 `supabase_v{n}_{설명}.sql` 파일만
  준비하고 운영자가 대시보드에서 수동 실행해야 진행된다.
- **Production 학생 로그인/PIN API WRITE 금지(에이전트 세션 한정)**:
  실제 학생 계정으로 로그인해야만 확인 가능한 화면(예: 2.5D 화면 실제
  진입)은 에이전트가 Production에서 직접 열 수 없다 — 운영자 실기기
  확인이 필요하다.
- **Vercel Preview 설정 변경 권한 없음(에이전트 세션 한정)**: 현재
  Preview는 SSO로 보호돼 있어 에이전트는 GET-only만 가능하다. 공유
  가능 링크 발급 등 설정 변경은 운영자만 할 수 있다
  (`DECISIONS_PENDING.md`의 "Vercel Preview 보호 설정" 항목 참고).
- **보호 대상 파일(다른 에이전트 소유) 수정 금지**: 이 세션(179차)에서
  문서 담당 에이전트가 건드릴 수 있는 파일은 `handoff.md`,
  `TESTING.md`, 신규 설계 문서, 이 파일, `DECISIONS_PENDING.md`,
  `.ai-status/` 신규 상태 파일로 한정된다. `src/`, `scripts/`,
  `tests/`는 병행 작업 중인 다른 에이전트 소유이며 문서 담당은 읽기만
  한다(`CLAUDE.md` 규칙 16).
- **git 명령 금지(이 세션의 문서 담당 한정)**: 문서 담당 에이전트는 이
  세션에서 git 명령을 실행하지 않는다 — 커밋/스테이징은 코드 담당
  에이전트 또는 lead가 처리한다.

이 표준 제약 중 어느 하나가 실제로 특정 작업을 막고 있다면, 위 "현재
활성 블로커" 절에 무엇이 막혔는지/누구 결정이 필요한지 구체적으로
추가한다.
