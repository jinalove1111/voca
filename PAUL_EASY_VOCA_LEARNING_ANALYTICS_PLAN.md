# PAUL_EASY_VOCA_LEARNING_ANALYTICS_PLAN.md — 5계층 Learning Analytics 설계

_작성: 2026-07-26. **순수 설계 문서 — 코드/SQL을 이 세션에서 전혀
작성·실행하지 않았다.** `PAUL_EASY_VOCA_AI_LEARNING_ENGINE_PLAN.md`
(약점분석/오답패턴/성장Score/AI Teacher 원본 설계), `PAUL_EASY_VOCA_
SAAS_ARCHITECTURE_PLAN.md`(§9 Dashboard 전략), `PAUL_EASY_VOCA_
MASTER_PLAN.md`를 종합한다. **이 문서는 "누가 무엇을 보는가"(청중별
Analytics)를 조직하는 문서**이고, 지표의 계산 방법·데이터 출처 원본은
`AI_LEARNING_ENGINE_PLAN.md`에 이미 설계돼 있으므로 반복하지 않고
인용한다 — 이 문서의 새 기여는 (a) 5개 청중별 노출 수준 차등화,
(b) 복습효과·학습지속률 지표 신규 정의, (c) 위험학생/이탈 예측
모델 설계다._

**핵심 원칙(전 섹션 공통)**: **같은 데이터라도 누가 보느냐에 따라
노출 형태가 달라야 한다.** 교사가 보는 "위험 확률 62%"를 학생이나
학부모에게 그대로 보여주면 안 된다 — 이 원칙이 아래 5개 대시보드
설계 전체를 관통한다.

---

## 1. 학생 Learning Dashboard

**목적**: 학생 스스로 "내가 늘고 있다"를 체감(`PROJECT_PAUL_GOAL.md`
VisibleGrowth 축) — **절대 평가/예측 지표를 그대로 노출하지 않는다.**

| 보여주는 것 | 원본 지표(§분석지표) | 표현 방식 |
|---|---|---|
| 이번 주 새로 안 단어 수 | 단어 기억률 | 숫자 + 단어 목록(성취감) |
| 곧 다시 만날 단어 미리보기 | 복습 효과(§4)의 스케줄 부분 | "내일 3개 단어를 다시 만나요" — SRS 박스를 직접 노출하지 않고 자연스러운 문장으로 |
| 이번 시즌 성장 Score | `AI_LEARNING_ENGINE_PLAN.md` §7 | 유지율 중심, "확실히 아는 단어"가 늘어나는 그래프 |
| 오늘/이번 주 학습 리듬 | 학습 지속률(§4) | 캘린더/스트릭(기존 UI 재사용) |

**절대 보여주지 않는 것**: 다른 학생과의 비교/순위, "위험 학생"/
"이탈 예측" 같은 성인 전용 지표(§6·§7) — 이 두 지표는 학생 화면에
존재 자체가 노출되지 않는다(계산은 서버에서, 결과는 교사/원장 레벨
에서만 소비).

---

## 2. 학부모 Report Dashboard

**목적**: 부모가 "우리 아이가 잘 크고 있다"를 압박 없이 확인
(`PAUL_PRINCIPLES.md` §7 원칙).

| 보여주는 것 | 원본 지표 | 표현 방식 |
|---|---|---|
| 학습 지속률(§4) | 원본 | "이번 주 4일 학습했어요" — 절대적 수치, 반 평균과 비교 안 함 |
| 오답 패턴 요약 | `AI_LEARNING_ENGINE_PLAN.md` §3 | "지금 연습 중인 것" 프레이밍(격려 톤) |
| 성장 Score 추세 | 동 §7 | 자기 자신의 지난 기간과만 비교 |
| **위험/이탈 신호(§6·§7)** | 원본은 확률 점수 | **원본 수치를 절대 그대로 노출하지 않는다** — "요즘 조금 뜸해요, 같이 응원해주세요" 같은 완곡한 문장으로만 변환. 확률/점수/등급 표기 금지 |

**설계 판단**: 위험/이탈 예측이 학부모에게까지 내려갈 때는 **원본
확률값이 아니라 "관찰됨/관찰 안됨" 같은 이진 신호를, 그것도 항상
긍정적 행동 제안과 함께**만 전달한다 — 아동에 대한 예측 모델 결과를
가정에 그대로 통보하는 것은 이 앱의 정서적 안전 원칙(`PROJECT_PAUL_
GOAL.md`)에 어긋난다.

---

## 3. 선생님 Teacher Dashboard

**목적**: 수업/개입 결정을 위한 **실무 수준** 정보 제공 — 교사는
성인 전문가이므로 학생/학부모보다 더 원본에 가까운 데이터를 볼 수
있다(단, 여전히 §6.4의 "사람이 최종 판단" 원칙 적용).

| 보여주는 것 | 원본 지표 | 활용 |
|---|---|---|
| 반 전체 오답 히트맵 | 오답 패턴(반 전체 관점, `AI_LEARNING_ENGINE_PLAN.md` §3.2) | 다음 수업에서 다시 설명할 단어/문법 결정 |
| 복습 효과(§4) | 원본 | "복습을 실제로 한 학생 vs 안 한 학생"의 정답률 차이 — 복습 정책이 실제로 먹히는지 확인 |
| **위험 학생 리스트(§6)** | 원본 근거(어떤 신호가 왜 떴는지)까지 표시 | 근거 없는 "위험" 딱지가 아니라 "최근 7일 미접속 + 정답률 하락" 식으로 **왜** 뜬 것인지 함께 보여줌 — 교사가 직접 확인 후 행동 |
| **학습 이탈 예측(§7)** | 원본 신뢰도 표시 | 조기 개입(학부모 상담 타이밍 등) 판단 자료 — 자동 알림 발송 절대 안 함(교사가 확인 후 수동으로만 소통) |
| 교재/단어 난이도 편차 | `docs/reading/06-exam-importance-standards.md`류 참고 + 실측 오답률 | 커리큘럼 조정 참고 |

---

## 4. Academy Owner Dashboard

**목적**: 학원 운영 의사결정(교사 배치, 반 편성, 결제/플랜) —
`PAUL_EASY_VOCA_SAAS_ARCHITECTURE_PLAN.md` §9.2가 이미 설계한
"결제 상태" 탭에 **Analytics 탭을 추가**하는 구조.

| 보여주는 것 | 근거 |
|---|---|
| 반별 성장 Score 비교 | 반 간 비교는 원장에게는 정당한 정보(학생 간 비교와 다른 레벨) — 교사 배치/반 편성 결정에 필요 |
| 학원 전체 학습 지속률·이탈률 추세 | §4·§7 집계, 학원 단위 롤업 |
| 교재별 효과 비교(다중 교재 사용 시) | `student_class_assignments`/`class_textbooks`(0004 결정) 기준 집계 |
| 결제/사용량(기존 설계) | `SAAS_ARCHITECTURE_PLAN.md` §7·§9.2 그대로 |

**설계 원칙**: 개별 학생 이름이 붙은 원본 위험/이탈 데이터는 원장
화면에서도 **교사 화면과 동일한 수준까지만**(더 넓히지 않음) — 원장이
학원 전체를 보되, 그렇다고 모든 개별 학생 세부사항을 실시간으로
감시하는 화면을 만들지 않는다(교사가 실제 담당자).

---

## 5. Platform Admin Dashboard

**목적**: 플랫폼(전 학원) 건강도 파악 + 콘텐츠/제품 개선 인사이트.
`SAAS_ARCHITECTURE_PLAN.md` §9.1(전체 학원 수/AI비용/매출/모니터링)의
**Analytics 확장판**.

| 보여주는 것 | 근거 |
|---|---|
| 학원 간 비교(익명화) | `product_events` 패턴처럼 학원 식별은 유지하되 **학생 개인 식별은 절대 노출 안 됨** — "학원 평균 이탈률" 같은 집계만 |
| 전 플랫폼 단어 난이도 랭킹 | 여러 학원의 `writing_answer_statistics` 집계 → 특정 교재/단어가 구조적으로 어려운지(교재 콘텐츠 품질 이슈) 판별 → 콘텐츠 개선 인사이트로 연결(`AI_LEARNING_ENGINE_PLAN.md` §10.1의 "학원 간 익명 집계 → 커리큘럼 개선" 흐름) |
| 어떤 학습 방식이 전반적으로 효과적인지(§AI 활용 3번) | 코호트 단위 관찰(§AI 활용 참고 — 개별 학원 규모로는 통계적 유의성 확보가 어렵고, 다학원 규모에서 비로소 의미 있어짐) |
| 플랫폼 전체 건강도(오류/모니터링) | `SAAS_ARCHITECTURE_PLAN.md` §9.1 그대로 |

---

## 분석 지표 정의 (7개)

### 1. 단어 기억률

`AI_LEARNING_ENGINE_PLAN.md` §7.2 "유지율"과 동일 지표를 이 문서에서는
"기억률"로 재호칭 — **정의를 두 곳에서 다르게 만들지 않는다**. 계산:
`mastered` 판정된 단어 중, 일정 기간 후 재등장 시에도 정답을 유지하는
비율.

### 2. 오답 패턴

`AI_LEARNING_ENGINE_PLAN.md` §3 그대로(단순오타/동의어/굴절/명백한
오답/애매 5분류, 99건 실측 비율 근거).

### 3. 복습 효과 (신규 정의)

**정의**: SRS 복습 추천(§4, `AI_LEARNING_ENGINE_PLAN.md` §4)을 받고
실제로 복습을 완료한 학생군의 재시험 정답률과, 복습을 안 한(또는
추천 자체가 없었던) 상태의 정답률을 **같은 학생의 시간대 전후로**
비교(반 평균과의 횡단 비교가 아니라 **자기 자신의 종단 비교** —
`PROJECT_PAUL_GOAL.md` 자기비교 원칙과 일관).

```
복습효과(학생, 단어) = 복습 후 정답률 − 복습 전 정답률
```

이 값이 체계적으로 낮으면(복습해도 효과 없음) → SRS 간격/난이도 설계
자체를 재검토할 신호(Learning Science Agent 역할, `AI_LEARNING_
ENGINE_PLAN.md` 연동).

### 4. 학습 지속률 (신규 정의)

**정의**: 기존 `product_events` 기반 리텐션 대시보드("무엇이 아이를
돌아오게 하나", 1/3/7일 복귀율)가 **이미 만들어져 있다** — 새 지표를
발명하지 않고 이 기존 집계를 개인/반/학원 단위로 재사용한다. 단,
`product_events`는 익명 설계이므로 **개인별 화면(§1 학생, §2 학부모)
에는 `student_daily_progress`의 `last_studied_date` 기반 스트릭을,
집계 화면(§5 플랫폼)에는 기존 `product_events` 복귀율을** 각각 다른
소스로 사용 — 목적에 맞는 소스를 섞지 않는다(설계 원칙).

### 5. 성장 Score

`AI_LEARNING_ENGINE_PLAN.md` §7 그대로(XP와 분리, 숙달률+유지율+향상
추세+복습 성실도).

### 6. 위험 학생 예측

아래 "예측 모델 설계" 절 참고.

### 7. 학습 이탈 예측

아래 "예측 모델 설계" 절 참고.

---

## AI 활용 — 예측 모델 설계

### 설계 전제 — 지금 데이터 규모에서 정직하게 접근한다

**111명, 학원 1곳** 규모는 진짜 머신러닝 모델(딥러닝 등)을 학습시키기엔
너무 작다 — 오히려 **소규모 데이터에서 무리하게 모델을 돌리면 과적합/
거짓 확신만 만든다.** 그래서 이 설계는 데이터 규모에 따라 3단계로
나눈다(무료·최소 인프라 우선 원칙, CLAUDE.md 규칙 7과 직결).

| 단계 | 데이터 규모 | 방법 | 인프라 |
|---|---|---|---|
| **1단계(지금 착수 가능)** | 학원 1~수 곳 | **규칙기반 임계값**(if-then, ML 아님) — 아래 상세 | 순수 함수, 새 인프라 0 |
| **2단계**(다학원 확장 이후) | 수십 학원, 수천 명 | 가벼운 통계 모델(로지스틱 회귀급) — Supabase Edge Function 안에서 순수 계산으로 충분, 별도 ML 플랫폼 불필요 | 여전히 무료/저비용 |
| **3단계**(정말 필요해질 때만) | 100학원+, 수년치 데이터 | 본격 ML 검토 — **이 시점에도 딥러닝보다 단순 회귀부터** | 이 규모에 실제로 도달했을 때 재검토, 지금 설계하지 않음(과설계 방지) |

**이 문서는 1단계만 구체적으로 설계한다** — 2·3단계는 "그때 가서
데이터로 재검토"가 정직한 답이다.

### 예측 질문 1 — 어떤 학생이 도움이 필요한가 (위험 학생 예측)

**1단계 규칙(전부 기존 데이터에서 계산 가능, 신규 수집 불필요)**:

```
위험_신호(학생) =
    (최근 7일 접속일수 == 0)
 OR (최근 2주 정답률이 그 이전 2주 대비 20%p 이상 하락)
 OR (추천된 복습 완료율 < 30%, 최근 2주 기준)
 OR (연속 3일 이상 오늘의 미션 미완료)
```

넷 중 **하나라도 해당하면** "관심 필요" 플래그(교사 화면에만, §3) —
숫자 점수가 아니라 **어떤 조건이 왜 걸렸는지 그대로 보여준다**(블랙박스
모델이 아니라 설명 가능한 규칙이 이 단계에서는 오히려 더 신뢰할 수
있다).

### 예측 질문 2 — 어떤 단어가 어려운가

**우선순위**: 이미 데이터가 있는 단어는 **예측할 필요가 없다** —
`writing_answer_statistics`/`word_status`의 **실측 오답률을 그대로
쓴다**(예측은 항상 실측보다 부정확하므로, 실측이 있으면 실측 우선).

**예측이 필요한 경우는 딱 하나 — 신규 등록 단어(콜드스타트)**:
아직 학생이 아무도 안 배운 단어의 예상 난이도. 규칙기반 휴리스틱:

```
예상난이도(신규단어) 가점 요소:
  + 음절/글자 수가 김
  + 같은 유닛 내 철자가 비슷한 다른 단어 존재(편집거리 2 이하)
  + 불규칙 형태(품사 변형이 규칙적이지 않음)
```

이 값은 **관리자에게 "이 단어는 조금 더 신경써서 설명이 필요할 수
있어요" 정도의 참고용**이지 확정 판정이 아니다 — 실제 학생 데이터가
쌓이면(1~2주 내) 즉시 실측값으로 대체된다.

### 예측 질문 3 — 어떤 학습 방식이 효과적인가

**이것은 순수 예측이 아니라 관찰적 비교 문제**라는 것을 먼저
분명히 한다 — "예측 모델"보다 "코호트 비교 분석"에 가깝다.

- 1단계(지금)에서는 **엄밀한 인과관계를 주장하지 않는다.** 예를 들어
  "Interleaving을 켠 학생이 안 켠 학생보다 기억률이 높다"는 관찰이
  나와도, 그 학생들이 애초에 더 성실했을 수도 있다(교란변수) — 지금
  규모(단일 학원)에서는 이런 교란을 통계적으로 분리할 표본이 부족.
- **진짜 답을 얻으려면**: 신규 학생을 무작위로 두 그룹(예: Interleaving
  ON/OFF)에 배정해 비교하는 A/B 방식이 필요 — 이건 다학원 규모(2단계
  이후)에서 표본이 충분해졌을 때 Learning Science Agent가 설계할
  실험이지, 지금 데이터로 답을 낼 수 있는 질문이 아니다.
- 지금 할 수 있는 것: 방법론적 결론이 아니라 **방향성 있는 관찰**
  ("이 학생군은 이 방식을 더 많이 씀 + 이런 결과가 나타남")까지만
  기록하고, 정책 변경의 근거로 단독 사용하지 않는다.

### 예측 결과 사용 원칙 (3개 질문 공통, 매우 중요)

1. **자동화된 결과 발송 금지** — 위험/이탈 신호가 학부모 알림을
   자동으로 트리거하지 않는다. 항상 교사가 먼저 확인(§3)한 뒤 사람이
   판단해 소통한다 — 기존 쓰기 AI 보조가 "미리보기만, 자동 반영 없음"
   원칙을 지키는 것과 동일한 안전장치.
2. **설명 가능성 우선** — 1단계는 전부 if-then 규칙이라 "왜 이 학생이
   플래그됐는지" 교사가 바로 이해 가능. 나중에 통계모델(2단계)을
   도입해도 "어떤 요인이 크게 작용했는지" 설명 가능한 형태를 유지한다
   (블랙박스 모델을 아동 대상 시스템에 함부로 쓰지 않는다).
3. **False Positive를 감수하는 방향으로 설계** — "위험군이 아닌데
   플래그됨"의 비용은 교사가 한 번 더 확인하는 정도지만, "위험군인데
   안 걸림"의 비용은 실제 학생을 놓치는 것 — 그래서 임계값은 민감하게
   (넉넉하게 걸리는 쪽으로) 잡는다.

---

## 최종: 1년 후 AI 교육 플랫폼 데이터 구조

`AI_LEARNING_ENGINE_PLAN.md` §10.1 아키텍처에 이 문서의 5-Dashboard
계층을 얹은 최종 형태 — 새 파이프라인을 또 만들지 않고 그 문서의
`learning_events`를 그대로 데이터 스파인으로 재사용한다.

```
[학생 학습 행동]
        │
        ▼
learning_events(단일 스파인, AI_LEARNING_ENGINE_PLAN §1)
        │
        ├──► 실측 집계(§분석지표 1~5) ──► 그대로 사용, 예측 불필요
        │
        └──► 1단계 규칙기반 예측(§6·§7) ──► 근거와 함께 산출
                        │
                        ▼
        ┌───────────────┴───────────────┬───────────────┬──────────────┐
        ▼                               ▼               ▼              ▼
  §1 학생 Dashboard              §2 학부모 Dashboard  §3 교사 Dashboard  §4 원장 Dashboard
  (원본 수치 없음,                (완곡화·긍정프레이밍,  (근거 포함 원본,   (반/학원 집계,
   성장/성취 프레이밍)              절대수치 없음)         사람이 최종판단)   개인 위험데이터는
                                                                          교사 화면과 동일선까지만)
                                                              │
                                                              ▼
                                                    §5 Platform Admin Dashboard
                                                    (학원 간 익명 집계만,
                                                     개인 식별 정보 없음)
```

### 지키는 원칙 (1년 뒤에도 변하지 않아야 하는 것)

- 같은 원본 데이터라도 **청중에 따라 다른 형태로 가공**해서 보여준다
  (원본을 그대로 아래 계층에 흘려보내지 않는다).
- 예측은 **1단계(규칙기반)를 기본값으로 최대한 오래 유지** — 데이터
  규모가 실제로 그것을 넘어설 때만 다음 단계로.
- 위험/이탈 예측은 **항상 사람(교사)의 확인을 거친 뒤에만** 행동으로
  이어진다 — 자동화하지 않는다.
- "학습 방식 효과" 같은 인과적 질문은 지금 데이터로 답을 낼 수 없다는
  것을 정직하게 인정하고, 실험 설계가 가능해지는 시점(다학원 규모)까지
  단정하지 않는다.

---

## 관련 문서

`PAUL_EASY_VOCA_AI_LEARNING_ENGINE_PLAN.md`(지표 계산법 원본),
`PAUL_EASY_VOCA_SAAS_ARCHITECTURE_PLAN.md`(§7·§9 Dashboard/결제 원본),
`PAUL_EASY_VOCA_MASTER_PLAN.md`, `PAUL_EASY_VOCA_AI_AGENT_OS.md`,
`docs/research/student-engagement-psychology.md`, `PROJECT_PAUL_
GOAL.md`, `PAUL_PRINCIPLES.md`.
