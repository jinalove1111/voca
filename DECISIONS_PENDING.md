# 결정 대기 항목 (Decisions Pending)

> 운영자 결정이 필요한 항목만 모아두는 파일이다. 완료된 결정은 이
> 파일에서 지우지 않고 "결정됨" 항목으로 표시를 바꾸고 `handoff.md`에
> 결정 내용과 일시를 append한다(append-only 원칙, `CLAUDE.md` 규칙 13).
> 새 항목은 표 아래에 이어서 추가한다.

가장 최근 항목: Paul Town 2.5D 프로토타입 학생 파일럿 준비
(`docs/design/town/PROTO25D_PILOT_READINESS_2026-09-25.md` §2/§7 참고,
2026-09-25 179차 세션에서 정리).

| 항목 | 배경 | 선택지 | 영향 | 기본값(현재) | 결정자 |
|---|---|---|---|---|---|
| 키보드/스크린리더 이동 경로 | 프로토타입은 탭/클릭 전용이며 키보드로 캐릭터를 이동시킬 방법이 없다 | (a) 파일럿 전 구현 (b) 파일럿 범위에서는 보류하고 백로그로 이월 (c) 접근성 요구사항이 없는 좁은 파일럿 대상에 한해 별도 승인 후 보류 | (a)는 구현 범위 확대, (b)/(c)는 접근성 미달 상태로 학생에게 노출 | 미구현(보류 상태) | 운영자 |
| 포커스 트랩 / `aria-modal` | 2026-09-25 `role="region"`+`aria-label`은 추가했으나 오버레이가 열렸을 때 포커스를 가두지 않는다 | (a) 파일럿 전 구현 (b) 보류 | 스크린리더/키보드 사용자가 오버레이 밖으로 포커스가 새는 경험 | 미구현 | 운영자 |
| 모바일 HUD 정보 토글 탭 영역 (O1) | 좌상단 HUD 버튼 영역(대략 x 12–231, y 12–56px)이 이동 탭과 겹칠 수 있음 | (a) 버튼을 이동시킨다 (b) 히트박스를 축소한다 (c) 그 영역만 탭이 HUD로 우선 처리되도록 그대로 둔다(pass-through 없음) | (c)는 그 좁은 영역에서 이동 탭이 막힘 | (c) 현행 유지 | 운영자 |
| 학생 진입점 + 파일럿 반 지정 시점 | 학생이 이 화면에 도달할 UI가 아직 없고, `paulTown2_5d` 플래그는 전역 기본 `false` | (a) 특정 파일럿 반에만 진입점 노출 후 플래그 ON (b) 전체 공개 전 별도 허용목록(allowlist) 메커니즘 신설 | 파일럿 규모/되돌리기 난이도에 직접 영향 | 진입점 없음, 플래그 false | 운영자 |
| 기존 Pilot 허용목록과의 상호작용 | `paulTown2_5d`는 `paulTownV1`/`paulTownV2`와 완전히 독립 격리된 실험으로 설계됨(공유 상태/허용목록 상호작용 없음, `src/config/features.js` 주석) | (a) 독립 유지 (b) 기존 Pilot 허용목록 메커니즘에 편입 | (b)는 기존 파일럿 인프라 재사용 가능하나 격리 원칙 변경 | (a) 독립 유지 | 운영자 |
| @3x 자산 여부 | 현재 1x/@2x만 존재, 고밀도 디바이스는 @2x 업스케일로 표시됨 | (a) @3x 세트 추가 생성 (b) 파일럿 규모에서는 생략 | (a)는 추가 생성 비용/시간, (b)는 초고밀도 화면에서 약간의 흐림 | 없음(생략) | 운영자 |
| 애니메이션 cadence의 보행 속도 동기화 | 프레임 전환 주기가 고정값이고 실제 이동 속도와 연동되지 않음 | (a) stride 기반 동기화 구현 (b) 현행 고정 cadence 유지 | (a)는 추가 구현, (b)는 빠른/느린 이동 시 다리 움직임이 부자연스러울 수 있음 | (b) 현행 유지 | 운영자 |
| 레거시 `walk-side-b`(v1) 파일 삭제 여부 | `paul-walk-side-b.png`/`@2x`가 디스크에 보존만 되고 더는 참조되지 않음 | (a) 삭제 (b) 보존 유지(롤백 대비) | (a)는 저장공간 정리, (b)는 즉시 롤백 가능성 유지 | (b) 보존 | 운영자 |
| Vercel Preview 보호 설정(공유 가능 링크) | 현재 SSO로 보호돼 있어 에이전트는 GET-only만 가능하고 운영자 본인 기기 로그인이 필요함(§6 체크리스트) | (a) 현행 SSO 유지 (b) 파일럿 확인 편의를 위한 별도 공유 가능 링크(shareable link) 발급 | (b)는 접근 편의 증가, 보안 노출면도 함께 증가 | (a) SSO 유지 | 운영자 |
| V2 S16 항목17 자석 드래그 간헐 실패 — 별도 세션에서 5회 재현/분리 | 2026-09-25 179차 `npm run verify:e2e` run 2(`01450a0d`)에서 `[town-v2] S16[390x844,mouse] 자석 드래그 배치 항목17`이 FAIL. 이 테스트는 Paul Town **V2**(`paulTown2_5d`와 완전히 독립된 별도 실험) 코드를 검사하며, 179차 세션은 V2 코드를 전혀 수정하지 않았다 — 동일 코드가 run 1에서는 PASS(간헐적). `TESTING.md`의 기존 V2 드래그 측정 노트에도 유사 간헐성이 이미 기록돼 있다 | (a) 별도 세션에서 5회 이상 반복 실행해 재현율 측정 후 근본 원인 조사 (b) 기존 간헐 이슈로 그대로 추적만 계속 | (a)는 V2 작업 범위를 여는 별도 세션 필요, (b)는 근본 원인 미상 상태 지속 | 미조사(179차 범위 밖 — V2 무변경 원칙) | 운영자 |
