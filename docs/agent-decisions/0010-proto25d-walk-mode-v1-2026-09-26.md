# 0010 — Paul Town 2.5D "산책 모드" v1: 월드 확장 + 카메라 추적 (운영자 지정 설계, IMPLEMENTED·VERIFIED, 미커밋)

- DECISION ID: 0010
- DATE: 2026-09-26
- TASK ID: proto25d-walk-mode-v1
- TASK CLASS: C (설계는 운영자가 확정 → 협의체 설계 파도 생략, 구현 결과 재검토 수행)
- FEATURE / PROBLEM: 캐릭터가 마을을 걸으며 카메라가 부드럽게 따라가 마을을 구경할 수 있게 한다(현재는 월드=화면 한 장).

## OWNER REQUEST
2026-09-26: 기존 충돌·이동 유지, 월드를 화면보다 넓게, 캐릭터가 화면 중앙 근처에 유지되도록 카메라 추적, 월드 경계 밖 노출 금지, 모바일 터치·데스크톱 유지, depth/그림자/착석 무손상, 상점·구매·DB 제외, 플래그 3개 false, 360/390/412/1280 검증, 검증 전 commit/push/PR 댓글 금지.

## CURRENT PRODUCT CONTEXT
ground = 월드(100:190, aspectRatio), world-% 좌표 계약, 캐릭터 폭 8%·최소 40px, 이동은 650ms CSS transition 레그, ResizeObserver 기존, HUD는 ground 형제. O2(`WORLD_MIN_Y`) 적용 상태.

## ENGINEERING POSITION (planner)
ground를 월드로 유지 + 뷰포트 래퍼 + ground transform. 월드 크기 `min(vw, vh/1.9)×1.6`(100:190) — `vw×2`는 데스크톱에서 2560×4864·캐릭터 3~4배 비대. rAF lerp(ref 기록)는 캐릭터 CSS transition과 엘리먼트가 달라 충돌 없음. 토글 기본 ON + 기존 E2E는 헬퍼 한 줄로 OFF. STOP-RETURN 없음.

## CHILD EXPERIENCE POSITION (구현 결과 재검토)
APPROVE. 카메라 추적으로 마을이 한 장소처럼 느껴짐. 우려: 빈 연녹색 공간이 많아 "더 찾을 게 있다"는 신호 부족; 데스크톱 레터박스 경계가 월드와 같은 색이라 깨진 레이아웃처럼 보일 수 있음(선택적 코스메틱).

## GAME DESIGN POSITION (구현 결과 재검토)
SIMPLIFY. 랜드마크가 걸어야 드러나는 점은 탐험 보상으로 유효. 우려: 오브젝트 8개뿐이라 일회성 발견 후 빈 공간 이동 반복. 물질적 이슈: 경계에서 그냥 멈춰 "마을 끝"과 "고장"을 구분 못함 → 네 방향 끝 시각 단서(비네트/울타리/헤이즈) 요청(시각 전용).

## DEVIL'S ADVOCATE POSITION
해당 없음(운영자 지정 설계로 설계 파도 생략).

## MATERIAL DISAGREEMENTS
게임(가장자리 단서 필수) vs 운영자 범위("산책과 카메라 이동만"). UX와 게임은 데스크톱/경계 시각 처리에서 방향 일치, 우선순위만 다름.

## LOWEST-COMPLEXITY OPTION
채택안 그대로(단일 파일 순수 수학 + 화면 래퍼/토글/루프, 기존 로직 무변경).

## RISKS
E2E ground 상자 기준 탭이 화면 밖으로 나감(→ S17은 뷰포트∩ground 교집합만 탭, S1–S16은 OFF 고정). rAF 프레임마다 rect 읽기(→ 프로토타입 범위 수용, ref 캐시 후속). 데스크톱 월드 폭 < 뷰포트(→ 중앙 정렬, 알려진 한계). 메모리 부족 환경에서 E2E 크래시(환경).

## PRODUCT LEAD RESOLUTION (orchestrator, 1회)
ACCEPT(운영자 설계). 가장자리 시각 단서·데스크톱 폭 확장·rAF ref 캐시는 DEFER(운영자 후속 결정, O2 협의체의 경계 아트 DEFER와 일관). "지정된 길"은 기존 BFS 격자로 해석.

## ACCEPTANCE CRITERIA
- [x] 4뷰포트에서 월드>뷰포트(데스크톱은 세로), 카메라가 월드 밖을 노출하지 않음(초기·도착 후, S17 + 실측 boundsOk)
- [x] 캐릭터가 도착 후 뷰포트 안(중앙 근처 또는 경계 clamp)
- [x] 이동·충돌·벤치 착석·depth·그림자·애니메이션 무변경(관련 파일 diff 없음, 기존 스위트 PASS)
- [x] 모바일 터치/데스크톱 마우스(S17 4뷰포트, walkShow)
- [x] 플래그 3개 false, DB/SQL WRITE 0, 상점/구매 미구현
- [x] 단위·build·verify:e2e 1825/1825·verify:all 필수 도메인 PASS
- [x] 콘솔 오류/미mock 0

## IMPLEMENTATION SCOPE
`camera.js`(신규), `testProto25dCamera.mjs`(신규), `registry.mjs`(+1), `Proto25DScreen.jsx`, `townProto25d.spec.mjs`(setDeviceFlags 1줄 + S17).

## OUT OF SCOPE
경계 아트/비네트, 도로 전용 이동 제약, 상점·구매·저장, 학습 연결, 오브젝트 밀도, features.js.

## QA PLAN / RESULT
단위 6스위트, build, verify:e2e, verify:all, 4뷰포트 실측 스크린샷. 결과: 코드리뷰 APPROVE, UX APPROVE, 게임 SIMPLIFY(후속), QA PASS. 상세 `handoff.md` 2026-09-26(182차).

## OWNER APPROVAL REQUIRED
구현 승인 2026-09-26 완료. 커밋·push·PR 댓글·머지는 운영자 지시 대기.

## TASK ENVELOPE
```
REPO: C:\voca
WORKTREE: C:/Users/jinal/AppData/Local/Temp/claude/C--voca/d95369ce-02f1-41e7-9e92-41e2cb3ce37a/scratchpad/wt-clean-pr
BRANCH: feat/paul-town-v2-clean-pr
BASE_COMMIT: 5a2f6c71
TASK_ID: proto25d-walk-mode-v1
TASK_CLASS: C
ALLOWED_PATHS: 위 IMPLEMENTATION SCOPE + handoff.md, 이 ADR, docs/design/town/PROTO25D_NEXT_STEPS_2026-09-23.md, .ai-status/orchestrator-proto25d-walk-mode-v1.json
```

## IMPLEMENTED-RESULT REVIEW
- child-experience-designer: APPROVE(데스크톱 레터박스 경계 구분 — 선택적).
- game-designer: SIMPLIFY(가장자리 시각 단서 요청 → DEFER, 운영자 결정).
- 수정 사이클: 0/2.

## FINAL STATUS
ACCEPTED → IMPLEMENTED → VERIFIED (2026-09-26, 미커밋 — 운영자 지시 대기)
