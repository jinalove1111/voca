# PAUL_EASY_VOCA_CUSTOMER_OPERATION_PLAN.md — 1인 운영자의 100학원 고객 운영 설계

_작성: 2026-07-26. **순수 설계 문서 — 코드/SQL/DB를 이 세션에서 전혀
변경하지 않았다**(이번 요청에 명시적 제약 문구는 없었지만, 이 대화
전체가 일관되게 설계 문서만 다뤄왔고 이 요청 자체도 "구조 설계"이므로
동일하게 처리했다). `PAUL_EASY_VOCA_MVP_ROADMAP.md`(단계별 규모),
(구 `PAUL_EASY_VOCA_AI_AGENT_OS.md`의 9-Agent 개발 조직 개념 — 삭제됨,
CLAUDE.md 헌법/DEVELOPER_GUIDE.md 워크플로우로 대체), `docs/agent-
decisions/0006-multitenant-saas-architecture.md`(§9 Dashboard, 구
SAAS_ARCHITECTURE_PLAN.md는 2026-07-31 병합됨), `PAUL_EASY_VOCA_BUSINESS_
MODEL_PLAN.md`(§4.3 "진짜 원가는 사람")를 기반으로 한다._

## 이 문서와 `AI_AGENT_OS.md`의 관계 — 두 조직은 다른 조직이다

`AI_AGENT_OS.md`의 9-Agent(CTO/PM/Learning Science/Developer/
Database/Security/QA/UX/Cost Optimization)는 **제품을 만드는 개발
조직**이다. 이 문서의 4-Agent(고객지원/교육/문서/QA)는 **이미 만들어진
제품으로 100개 학원을 운영하는 고객 운영 조직**이다 — 목적이 다르다.
**QA Agent만 두 조직에 공통으로 등장**하는데, 이건 같은 역할을
재사용하는 것이지 별개 Agent를 새로 만드는 게 아니다(§AI Agent 활용
4번에서 상세).

## 핵심 원칙 — "혼자"가 가능해지는 4가지 축

1. **표준화** — 모든 학원이 같은 절차를 거친다(예외를 최소화해야
   1인이 감당 가능한 반복작업으로 줄어든다).
2. **자동화** — 반복적인 판단·응답을 AI Agent가 흡수한다.
3. **셀프서비스** — 고객(원장/교사)이 스스로 할 수 있는 일을 최대한
   늘린다(사람이 대신 해주는 일을 줄인다).
4. **에스컬레이션 기준 명확화** — "언제 사람(운영자 본인)이 개입해야
   하는지"를 모호하지 않게 정의한다.

**규모별 자동화 수준은 다르다** — `MVP_ROADMAP.md`의 Phase 구분을
그대로 따른다: Phase 2(5곳)는 수동으로도 감당되지만, Phase 3(20곳)
부터 자동화가 필수, Phase 4(100곳)는 자동화 없이는 물리적으로 불가능
— 아래 각 섹션에 이 페이싱을 명시한다.

---

## 1. 학원 가입 과정

| 단계 | 내용 | 자동화 수준(Phase별) |
|---|---|---|
| 1 | 랜딩페이지 → 가입폼(학원명/연락처/예상 학생 수) | 전 Phase 자동 |
| 2 | 요금제 선택 — **Free 체험이 기본 진입**(`BUSINESS_MODEL_PLAN.md` §2, 카드 없이 시작) | 전 Phase 자동 |
| 3 | `academies` 레코드 생성 + Owner 계정 발급(Supabase Auth, `SAAS_ARCHITECTURE_PLAN.md` §3.4) | Phase 2: 운영자 수동 승인 / Phase 3+: 완전 자동 |
| 4 | 온보딩 이메일(다음 단계 안내) 발송 | 교육 Agent가 콘텐츠 생성, 발송은 자동 트리거 |

**Phase 2(5곳)에서 수동 승인을 두는 이유**: 초기엔 어떤 학원이 실제로
적합한 고객인지 운영자가 직접 검증하는 것이 안전(스팸/부적합 가입
필터링) — Phase 3부터는 이미 검증된 패턴이 쌓여 자동화로 전환.

---

## 2. 초기 세팅 과정

가장 사람 개입이 몰리는 지점(`BUSINESS_MODEL_PLAN.md` §4.3이 이미
"진짜 원가는 사람"이라고 지목한 바로 그 구간) — 그래서 여기가 AI
Agent 투입의 최우선 지점이다.

| 단계 | 내용 | AI Agent 개입 |
|---|---|---|
| 반/유닛 생성 | 기존 관리자 화면 그대로(신규 기능 아님) | 교육 Agent가 "다음에 뭘 해야 하는지" 맞춤 체크리스트 제공 |
| 단어 업로드 | 기존 엑셀/PDF 업로드 재사용 | 문서 Agent가 "엑셀 형식 예시" 등 셀프서비스 가이드 상시 제공 |
| 기본 설정(쓰기시험/게임화 on-off) | 기존 UI 그대로 | 교육 Agent가 추천 기본값 안내(예: "공부방은 게임화부터 켜는 걸 추천해요") |
| 온보딩 완료 확인 | `academy_settings`의 진행 상태 확인 | 완료 안 되면 교육 Agent가 후속 안내 재발송(사람 개입 없이) |

**24~48시간 내 완료 목표** — 이 시간 안에 완료 안 되면 고객지원
Agent가 먼저 말을 걸도록 설계(§5와 연결, 이탈 방지).

---

## 3. 학생 등록

- 기존 자산 재사용: 관리자가 학생 개별 등록(`addStudent`) + 임시 PIN
  일괄 발급(`bulk-generate-temp-pins` 계열, 이미 `api/admin-pin-
  actions.js`로 통합돼 있음) + 학생 자기 PIN 설정(`pin_setup_allowed`)
- **신규로 필요할 수 있는 것(설계 관점 식별, 구현은 별도 작업)**: 지금
  구조는 학생 1명씩 등록하는 흐름이 기본이라, 학원이 50~100명을
  한 번에 옮겨올 때(특히 타 서비스에서 마이그레이션하는 경우) **CSV
  일괄 등록** 기능이 없으면 온보딩 병목이 된다 — Phase 3(20곳) 규모
  부터는 이 갭이 실제로 체감될 가능성이 높다는 점을 여기 기록해둔다
  (이 문서는 설계만, 실제 구현 필요 여부는 CTO Agent가 실사용 데이터로
  판단).
- 학생 PIN 설정은 항상 학생/학부모 셀프서비스(관리자가 대신 입력하지
  않음) — 기존 `pin_setup_allowed` 플로우 그대로.

---

## 4. 선생님 교육

**정기 웨비나 방식은 "혼자 운영"에 맞지 않는다** — 시간이 사람 수에
비례해 소모된다. 대신:

- **자가학습 콘텐츠 우선**: 짧은 인앱 가이드 투어 + 문서(FAQ) + 영상
  1회 제작(반복 재생) — 교육 Agent가 이 콘텐츠를 학원의 실제 세팅
  상태에 맞춰 개인화해서 안내(예: "게임화를 껐네요, 이런 학원은
  대개 이런 이유로 꺼요" 류 맥락 제공).
- **셀프서비스 실패 시에만 사람 개입**: 교육 Agent 안내를 따라도 안
  풀리는 경우만 §5(고객 문의 처리)로 에스컬레이션.

---

## 5. 고객 문의 처리

```
문의 접수
   │
   ▼
1차: 고객지원 Agent — FAQ/문서 자동 매칭, 답변 초안 생성
   │
   ├─ 확신도 높음(명확한 FAQ 매칭) → 자동 발송(Phase 3+부터,
   │                                   Phase 2까지는 사람 검토 후 발송)
   │
   └─ 확신도 낮음/민감 액션(결제/계정삭제/데이터 이슈) →
                                        2차: 운영자 본인 에스컬레이션
```

**SLA(요금제 연동, `BUSINESS_MODEL_PLAN.md` §2 요금제와 일관)**:
Starter/Professional은 이메일 기준 48시간 내, Premium/Enterprise는
우선 처리(구체 시간은 파일럿 검증 후 확정).

**절대 완전 자동화하지 않는 것**: 결제/환불/계정 삭제/데이터 이슈
관련 문의는 고객지원 Agent가 초안만 만들고 **항상 운영자 확인 후
발송** — `AI_DEVELOPMENT_PROTOCOL.md` §8의 "자동화된 결과 발송 금지"
원칙과 동일한 이유(민감 액션은 사람이 최종 판단).

---

## 6. 장애 대응

```
모니터링(SaaS TOP10 10번, 무료 알림) → 이상 감지
        │
        ▼
운영자에게 알림 도착
        │
        ▼
대응 절차 실행 — v3_11 "1시간 런북" 패턴을 일반화한 표준 절차:
  ① 영향 범위 확인(전 학원 vs 특정 학원)
  ② 롤백 가능 여부 판단(코드 롤백 vs SQL 롤백, 각기 다른 트랙)
  ③ 영향받는 학원에 고객지원 Agent가 상황 안내 초안 자동 생성
     (발송은 운영자 확인 후 — §5와 동일 원칙)
  ④ 해소 후 사후 기록(handoff.md 패턴을 운영 장애에도 동일 적용)
        │
        ▼
문서 Agent가 이번 장애를 FAQ/공지사항으로 정리(재발 시 고객지원
Agent가 즉시 참고할 수 있도록)
```

**핵심 설계 판단**: 장애 대응 자체를 AI가 자동으로 "고치지" 않는다
(DDL 등 파괴적 작업을 Agent가 직접 실행하지 않는다는 `CLAUDE.md` 규칙
8과 동일한 정신) — AI Agent는 **소통·기록을 돕고, 실제 조치는 운영자가
한다.**

---

## 7. 업데이트 배포

`AI_DEVELOPMENT_PROTOCOL.md`의 7단계 Workflow(개발 조직 쪽)가 이미
다루는 "기능 하나를 어떻게 만드는가"와 달리, 여기서는 **완성된 업데이트
를 100개 학원에 어떻게 내보내는가**를 다룬다.

| 구분 | 코드 배포 | DB 마이그레이션 |
|---|---|---|
| 실행 주체 | Vercel 자동(git push) | **운영자 수동**(CLAUDE.md 규칙 8, 몇 학원이든 예외 없음) |
| 영향 범위 | 전 학원 동시(단일 프로젝트, `SAAS_ARCHITECTURE_PLAN.md` §5.2 RLS 분리 구조) | 전 학원 동시 — **한 번의 마이그레이션 실수가 100개 학원에 동시 영향**을 준다는 것이 다학원 규모에서 가장 큰 배포 리스크 |
| 사전 검증 | QA Agent가 build/verify(기존 절차) | QA Agent가 **스테이징 환경에서 먼저 검증**(다학원 확장 이후 신규 절차 — Phase 3부터 필수) |
| 고객 공지 | 마이너 변경은 공지 없음, 메이저 변경은 문서 Agent가 릴리즈노트 작성 | 다운타임 가능성 있는 변경은 사전 공지(학원 운영시간 회피한 배포 윈도우) |

---

## AI Agent 활용 설계

### 고객 지원 Agent

- **역할**: 1차 문의 응대(FAQ/문서 자동 매칭, 답변 초안 생성)
- **책임**: §5의 1차 응답, §6의 장애 상황 안내 초안, 온보딩 지연
  학원에 선제적 연락 초안
- **입력 자료**: 고객 문의 원문, 기존 FAQ/문서(문서 Agent가 유지),
  해당 학원의 `academy_settings`/진행 상태
- **출력 결과**: 답변 초안(확신도 표시 포함) — 확신도 높고 민감하지
  않은 문의만 Phase 3+에서 자동 발송, 그 외 전부 사람 확인
- **협업 방식**: 문서 Agent가 만든 FAQ를 소비, 해소 안 되면 운영자에게
  에스컬레이션, 반복되는 신규 문의 패턴은 문서 Agent에게 "이거 FAQ로
  만들어야 함" 피드백
- **금지 행동**: 결제/환불/계정삭제/데이터 이슈의 **완전 자동 처리**
  (초안까지만), 학생 개인정보를 문의 응대 과정에서 노출, 확신 없는
  답변을 확신 있는 것처럼 발송

### 교육 Agent

- **역할**: 신규 학원 온보딩(§2)과 교사 교육(§4) 콘텐츠를 그 학원의
  실제 상태에 맞춰 개인화 제공
- **책임**: 온보딩 체크리스트 생성, 진행 지연 시 후속 안내, 기능별
  가이드 투어 콘텐츠 관리
- **입력 자료**: `academy_settings`/온보딩 진행 상태, 문서 Agent가
  관리하는 가이드 원본 콘텐츠
- **출력 결과**: 맞춤 체크리스트, 인앱 안내 문구, 이메일 시퀀스 초안
- **협업 방식**: 문서 Agent의 콘텐츠를 소비(콘텐츠를 직접 새로 쓰지
  않음 — 원본은 문서 Agent 소유), 온보딩이 실패하는 지점을 발견하면
  고객지원 Agent에게 넘김
- **금지 행동**: 코드/설정 변경(안내만, 대신 클릭해주지 않음), 검증
  안 된 정보 생성(항상 문서 Agent의 최신 원본 기반으로만 답변)

### 문서 Agent

- **역할**: 고객 대상 문서(사용설명서/FAQ/릴리즈노트/공지사항)를
  유지·최신화. **`docs-maintainer`(기존, `AI_AGENT_OS.md`)와 다른
  대상**임을 명확히 한다 — `docs-maintainer`는 내부 개발 문서
  (`DATABASE.md` 등)를 append하고, 이 문서 Agent는 **고객이 직접 읽는
  콘텐츠**를 관리한다.
- **책임**: 기능 변경 시 사용설명서 갱신, 반복 문의 패턴을 FAQ로 전환
  (고객지원 Agent의 피드백 기반), §7의 릴리즈노트 작성
- **입력 자료**: 개발 조직(`AI_AGENT_OS.md`)의 배포 기록, 고객지원
  Agent의 반복 문의 패턴 리포트
- **출력 결과**: 최신 FAQ/가이드/릴리즈노트(고객 대상 문구 — 내부
  기술 용어를 그대로 노출하지 않고 학원 운영자가 이해할 수 있는 언어로
  변환)
- **협업 방식**: 고객지원 Agent·교육 Agent에게 원본 콘텐츠 공급(이
  둘은 콘텐츠를 직접 생성하지 않고 소비만), 개발 조직의 `docs-
  maintainer`가 남긴 기술 변경 기록을 고객향 언어로 번역
- **금지 행동**: 내부 개발 문서(`DATABASE.md`/`ARCHITECTURE.md` 등)
  수정(그건 `docs-maintainer` 소관), 검증되지 않은 기능을 마치 완료된
  것처럼 안내(배포 확인 전 릴리즈노트 발행 금지)

### QA Agent (기존 역할 재사용, 다학원 확장판)

- **역할**: `AI_AGENT_OS.md`의 QA Agent와 **동일 역할**을 재사용 —
  다만 다학원 운영 맥락에서 §7(업데이트 배포)의 **"스테이징 환경
  선검증"**이 추가 책임으로 얹힌다.
- **책임**: 기존(build/verify 독립 재실행, Code Review Checklist)에
  더해 — DB 마이그레이션이 여러 학원에 동시 영향을 주는 변경일 경우
  프로덕션 적용 전 스테이징에서 먼저 실행해 회귀 확인
- **입력/출력/협업/금지**: `AI_AGENT_OS.md` §7 QA Agent 정의 그대로
  — 여기서 새로 정의하지 않는다(중복 방지, 규칙 3과 같은 정신).

### 프로세스 × Agent 매핑

| 프로세스 | 주 담당 Agent | 보조 |
|---|---|---|
| 1. 가입 | (자동화 로직, Agent 불필요) | 교육 Agent(가입 직후 온보딩 시작) |
| 2. 초기 세팅 | 교육 Agent | 문서 Agent(가이드 원본) |
| 3. 학생 등록 | 교육 Agent | 고객지원 Agent(막힐 때) |
| 4. 선생님 교육 | 교육 Agent | 문서 Agent |
| 5. 고객 문의 | 고객지원 Agent | 문서 Agent(FAQ 원본) |
| 6. 장애 대응 | 운영자 본인(조치) | 고객지원 Agent(소통), 문서 Agent(사후 기록) |
| 7. 업데이트 배포 | QA Agent(검증) | 문서 Agent(공지) |

---

## 관련 문서

`PAUL_EASY_VOCA_MVP_ROADMAP.md`(Phase별 자동화 페이싱 원본).
(구 `PAUL_EASY_VOCA_AI_AGENT_OS.md`/`PAUL_EASY_VOCA_AI_DEVELOPMENT_
PROTOCOL.md`는 삭제됨 — CLAUDE.md 헌법/DEVELOPER_GUIDE.md 워크플로우로
대체) `docs/agent-decisions/0006-multitenant-saas-architecture.md`(§9
Dashboard, 구 SAAS_ARCHITECTURE_PLAN.md는 2026-07-31 병합됨),
`PAUL_EASY_VOCA_BUSINESS_MODEL_PLAN.md`(§4.3 "진짜 원가는
사람" — 이 문서 전체의 존재 이유).
