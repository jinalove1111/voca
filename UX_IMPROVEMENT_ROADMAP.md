# UX 개선 로드맵 (Learning Experience → Action)

_작성: 2026-08-02, learning-experience 세션. **코드 변경 없음** — 설계
문서. `LEARNING_EXPERIENCE_REPORT.md`의 진단을 실행 가능한 항목으로
전환한다. 모든 항목은 CLAUDE.md "제품 불변 조항"(새 XP 발생원/곱연산
배수/티켓 가챠/신규 화폐/스트릭 징벌/실제 손실/개인 순위 공개/페이투윈·
결제·푸시 금지, 하우스=티켓 합산, 교사 기능 opt-in, 학생 신규 기능은
운영자 승인, 홈 화면 80% 불변, 미션 4칸 고정, DB엔 사실만, 엔진 결정론,
문서 append-only) 안에서만 제안한다. **본 문서는 설계 제안일 뿐이며 실제
구현은 규칙 12(학생 대상 신규 기능/UI는 운영자 승인 필요)에 따라 별도
승인·별도 세션에서 진행한다.**

각 항목은 **문제 / 왜 중요한가 / 제안 해법 / 기대 학습 효과 / 개발
난이도(S·M·L) / 리스크 / 우선순위**를 표기하고, 마지막에 "지금 데이터로
검증 가능한가 / 어떤 계측이 있어야 검증되는가"를 한 줄로 덧붙인다.

---

## Critical (6건)

### C1. 간격 반복 도입 — "돌아오는 단어"

- **문제**: 한 번 맞힌 단어는 교사 재배정이나 유닛 재선택 없이는 다시
  나오지 않는다(**관측됨**, `spellingReviewQueue`는 정답 시 영구 삭제되는
  1일 큐뿐).
- **왜 중요한가**: 망각곡선 방치는 장기 파지 실패의 가장 직접적인 원인.
  이미 만들어진 엔진을 켜기만 하면 되는 가장 레버리지 높은 항목.
- **제안 해법**: `src/learning/memory/leitner.js`(박스 `[0,1,3,7,14,30]`)는
  이미 구현돼 있음. 배선 순서: (1) `wordLibrary.js`의
  `syncStudentProgress`가 `review_data` 컬럼을 통째로 덮어쓰는 1줄을
  `encodeReviewData(existingReviewData, state)` 기반 read-merge-write로
  교정(선행 조건, `reviewDataBackend.js` 주석에 이미 명시) → (2)
  `reviewDataBackend.js`를 실제 화면/훅에 배선 → (3) 기본 off 플래그로
  일부 학생/반에 우선 노출.
- **기대 학습 효과**: 간격 반복은 장기 파지 개선에 대해 학습과학
  문헌에서 가장 재현성 높은 효과 중 하나(**추정** — 이 앱 전용 효과
  크기는 측정 전이라 미확인).
- **개발 난이도**: M(엔진은 이미 있음, 저장 계층 수정 + 배선 + 화면
  1곳 신규 연결).
- **리스크**: 중간 — `review_data` 컬럼 스키마를 건드리는 작업이라
  기존 `spellingWrongToday` 저장과 충돌하지 않는지 회귀 테스트 필요.
  학생 신규 노출 화면이므로 규칙 12에 따라 운영자 승인 필요.
- **우선순위**: 최상위(다른 Critical 항목 대비 유일하게 "엔진은 이미
  존재"라 구현 대비 임팩트 비율이 가장 높음).
- **검증**: 지금 데이터로는 검증 불가(review_data가 애초에 저장되지
  않음) → 배선 후 "N일 뒤 재노출된 단어의 정답률" 이벤트 계측 필요.

### C2. 핵심 학습 플로우 계측

- **문제**: `product_events`가 수집하는 8종은 전부 부가 기능이고 핵심
  학습 플로우(단어/발음/퀴즈/쓰기) 이벤트가 0건(**관측됨**). 어느
  미션 슬롯에서 이탈하는지도 모른다.
- **왜 중요한가**: 이 항목이 막혀 있으면 다른 모든 개선안의 효과를
  사후 검증할 수 없다 — 개선 루프 자체가 닫혀 있는 메타 결함.
- **제안 해법**: 새 테이블 없이 기존 `product_events`(이벤트명 문자열
  + 최소 payload 구조로 추정)에 학습 단계 단위 이벤트를 추가하는 방향
  또는(payload 컬럼이 없다면) 이벤트명 자체를 단계별로 세분화하는
  방향 — 정확한 스키마는 `product_events` 테이블 구조 확인 후 결정
  (**미확인** — 이 문서 작성 시점에 테이블 컬럼을 직접 조회하지
  않았으므로, 구현 세션에서 먼저 `\d product_events` 급의 확인 필요).
  DB에는 사실만 저장한다는 제품 불변 조항에 따라 추정/가공값이 아닌
  원시 이벤트(예: `word_view_step_entered`, `mission_slot_completed`
  `{slot: 'pron'}`)만 기록.
- **기대 학습 효과**: 직접적 학습 효과는 없음 — 그러나 C1/C3/C4/C6 등
  다른 모든 항목의 효과 측정을 가능하게 하는 선행 인프라.
- **개발 난이도**: S~M(이벤트 발생 지점 삽입은 기계적이나, 지점이
  많음 — 6개 플로우 × 평균 2~3 지점).
- **리스크**: 낮음(읽기 전용 로깅, 학습 플로우 자체는 변경 없음) —
  단, 이벤트 과다 발생 시 쓰기 비용/성능 영향은 확인 필요.
- **우선순위**: 최상위(다른 항목의 ROI 판단 근거가 됨).
- **검증**: 이 항목 자체가 "검증을 가능하게 하는" 항목 — 구현 완료
  기준은 배선 후 실제 이벤트가 DB에 쌓이는지 스모크 확인.

### C3. 3/4 벽 해소 — 목표 근접성 가시화 + 4번째 슬롯 편중 검토

- **문제**: 3개 완료한 날(24.9%)이 4개 완료한 날(13.2%)의 1.9배
  (**관측됨**). 4번째 슬롯이 유일하게 마이크·용기가 필요한 발음
  슬롯이라는 점과 겹칠 가능성(**추정**, 인과 미확인).
- **왜 중요한가**: 목표 구배 효과(goal-gradient effect) — 목표에
  가까울수록 노력이 증가하는 현상이 활용되지 않고 있을 가능성.
  4/4 완료 시에만 지급되는 티켓(하루 유일한 티켓 발생원)까지 막혀
  하우스 시스템 전체가 정체되는 상류 원인이기도 함(→
  `GAME_SYSTEM_IDEAS.md` 참고).
- **제안 해법**: (a) 학습 중 화면에 "4칸 중 몇 칸"을 상시 노출(미션
  4칸 자체는 늘리지 않음 — 제품 불변 조항 준수, 표시만 강화). (b)
  발음 슬롯의 마찰 원인이 "마이크 권한/기기"인지 "부끄러움"인지
  분리 계측(C2 선행) 후, 부끄러움이 원인이면 강제 녹음 게이트 완화는
  별도 항목(H1)에서 다룬다 — 이 항목 자체는 게이트를 풀지 않고 가시성만
  개선.
- **기대 학습 효과**: 완료율 상승 자체가 직접적 학습량 증가로
  이어짐(더 많은 인출·발화 기회).
- **개발 난이도**: S(진행 표시 UI는 기존 데이터로 계산 가능,
  round.wordsViewed/examplesHeard/quizSolved/pronunciationOk가 이미 존재).
- **리스크**: 낮음(표시만 추가, 보상 로직 변경 없음).
- **우선순위**: 상(구현 난이도 대비 임팩트가 가장 높은 축에 속함).
- **검증**: C2 계측 이후 "슬롯별 완료 순서/이탈 지점"으로 가시화 전후
  비교 가능. 지금은 categories_completed가 정수 1개뿐이라 사전 검증 불가.

### C4. 퀴즈 난이도 보정 — 재인에서 회상으로

- **문제**: 퀴즈 정답률 95.6%(2,848/2,978, **관측됨**)는 바람직한
  어려움이 부재한 재인형 과제의 전형적 특징.
- **왜 중요한가**: 인출 강도가 약한 연습은 장기 파지 효과가 제한적.
- **제안 해법**: 문항 전부를 바꾸지 않고 일부를 "첫 글자 힌트 + 직접
  입력"류의 회상형으로 조정하거나, 오답 선택지 품질(그럴듯한 오답)을
  높여 재인 난이도 자체를 올림. 쓰기(스펠링) 플로우가 이미 산출형이므로
  완전히 새로운 과제 유형을 만들 필요는 없음 — 기존 쓰기 로직 재사용
  검토.
- **기대 학습 효과**: 인출 강도 상승 → 장기 파지 개선(**추정**, 학습
  과학 일반 원리).
- **개발 난이도**: M(문항 생성/오답 선택지 로직 변경, 기존 정답률
  95.6% 데이터가 새 기준선과 비교 어려워짐 — A/B 필요).
- **리스크**: 중간(체감 난이도 상승이 저학년 학생에게 좌절을 줄 수
  있음 — 8살 대상 제품이라 신중한 파일럿 필요, 제품 불변 조항상
  "스트릭 징벌 금지/실제 손실 금지"와 충돌하지 않는 선에서 설계).
- **우선순위**: 중상(효과는 크지만 파일럿·튜닝 기간 필요).
- **검증**: C2 계측 후 재인형 대비 회상형 문항의 1주/1개월 뒤 재시험
  정답률 비교(C1 간격 반복과 결합해야 측정 가능).

### C5. 숙제 플로우 실사용화

- **문제**: `daily_assignments` 1행, `word_ids` 빈 배열 — 교사가 숙제
  배정 기능을 관측 기간 내내 쓰지 않았다(**관측됨**).
- **왜 중요한가**: 배정이 없으니 학생은 유닛 전체 폴백으로 학습해
  난이도/스페이싱 조절 기능이 의미를 잃는다. 교사가 안 쓰는 기능은
  아무리 잘 설계돼도 효과가 0이다.
- **제안 해법**: 원클릭 "오늘 숙제 만들기" 버튼을 교사 기본 동선에
  배치(자동 생성 로직은 어제 이미 구현됨, `d130905`/관련 커밋 —
  `handoff.md` 참고). 새 기능을 만드는 것이 아니라 **이미 있는 자동
  생성 기능을 발견 가능한 위치로 옮기는** UX 작업.
- **기대 학습 효과**: 배정된 소량 세트가 실제로 쓰이면 스페이싱·난이도
  조절 효과가 비로소 발휘됨.
- **개발 난이도**: S(버튼 배치/동선 변경, 신규 로직 없음).
- **리스크**: 낮음(교사 화면 UX 변경, 학생 대상 아님 — 규칙 12 범위
  밖).
- **우선순위**: 상(난이도 S, 임팩트 큼 — ROI 최상위권).
- **검증**: 배치 전후 `daily_assignments` 신규 행 생성 빈도 비교로
  즉시 검증 가능(이미 존재하는 테이블).

### C6. 보상-학습 정렬 — 열람에서 인출로

- **문제**: XP 이벤트 word-view-complete(30.2%) + daily-mission-complete
  (29.5%, 이 중 3/4가 열람·듣기·퀴즈)가 인출/산출형(quiz-complete
  21.2%, writing-complete 12.5%)보다 훨씬 큰 비중(**관측됨**).
- **왜 중요한가**: "쉬운 행동이 가장 크게 보상받는" 구조는 학습-보상
  정렬을 어긋나게 한다.
- **제안 해법**: **새 XP 발생원을 만들지 않고**, 기존 XP 이벤트의
  금액 배분만 재조정(예: writing-complete 비중을 word-view-complete
  대비 상대적으로 올림) — 곱연산 배수 아님, 단순 상수 재조정. 단, 이는
  기존 학생이 이미 체감하고 있는 보상 경제를 바꾸는 것이라 **운영자
  승인 필수**이며, 변경 전후 총 XP 지급량이 인플레이션되지 않도록
  "총량 보존형 재배분"(A를 늘리면 B를 그만큼 줄임)만 허용.
- **기대 학습 효과**: 인출/산출 행동에 대한 동기 상승(**추정**).
- **개발 난이도**: S(상수 변경 수준) — 그러나 **경제 변경이므로
  운영자 승인 및 단계적 롤아웃 필요**.
- **리스크**: 중간-높음(보상 경제는 기존 학생이 이미 학습한 규칙 —
  급격한 변경은 규칙 1 "기존 플로우를 위험하게 하지 않는다"에 저촉
  가능. 반드시 소폭·단계적 조정 + 사전 운영자 승인).
- **우선순위**: 중(임팩트는 크지만 리스크가 가장 높아 신중히 순서
  배치 — C1~C5 이후 데이터가 쌓인 뒤 판단 권장).
- **검증**: C2 계측 이후 재배분 전후 quiz/writing 참여율 비교.

---

## High

### H1. 강제 녹음 게이트 완화

- **문제**: 발음/예문 단계가 녹음을 강제해야 다음 단계로 진행되는
  구조 — 부끄럼 많은 아이·마이크 권한 거부·조용한 교실 환경에서 학습
  자체가 막힘(**관측됨**, `NEXT_PRIORITY.md` 이미 보류 항목으로 등재).
- **왜 중요한가**: §1-3 분석에서 발음 플로우가 "부끄럼 많은 아이가
  편안할까"에 가장 취약한 지점으로 식별됨.
- **제안 해법**: "건너뛰고 나중에 다시 시도" 옵션 추가(학습 설계
  자체는 유지, 예외 경로만 추가) — 운영자 판단 필요(NEXT_PRIORITY.md에
  이미 "학습 흐름 자체를 바꾸는 결정"으로 명시돼 보류 중).
- **기대 학습 효과**: 이탈 감소 — 발음 연습량 자체는 줄 수 있어
  트레이드오프 존재.
- **난이도**: M. **리스크**: 중(핵심 학습 설계 변경). **우선순위**: 상.
- **검증**: 게이트 완화 전후 발음 슬롯 도달률/완료율 비교(C2 선행 필요).

### H2. "알아요" 자기평가 부정확성 보정

- **문제**: "알아요"를 누르면 그 단어의 나머지 단계(예문/퀴즈)를 전부
  건너뛴다(**관측됨**, `WordDetail.jsx` 주석 명시). 자기평가만으로
  전체 단계를 생략하는 것은 부정확할 수 있음(**추정** — 이 앱에서
  "알아요를 많이 쓴 학생이 발음을 회피했다"는 인과는 관측되지 않음,
  오히려 정반대 상관이 있어 신중한 해석 필요).
- **왜 중요한가**: 유능감 착각(자기평가 오류)이 실제 학습 기회 손실로
  이어질 수 있음.
- **제안 해법**: "알아요" 선택 시 곧바로 스킵하지 않고 1문항 확인
  퀴즈를 거친 뒤 스킵(생성 효과를 살짝 추가) — 기존 "알아요" 자체의
  존재/UX 톤은 유지.
- **난이도**: S. **리스크**: 낮음-중(기존 스킵 습관을 가진 학생의
  체감 마찰 증가 가능). **우선순위**: 중상.
- **검증**: 1문항 확인 도입 전후 해당 단어의 재출현 시 정답률 비교
  (C1 간격 반복과 결합 필요).

### H3. 복습 동선 4종 통합

- **문제**: 레벨업 미션 3회 정답 / SpellingReview 큐 / WordBrowser 필터
  / GuidedSession 1회 재시도 — 서로 다른 규칙의 복습 동선 4개가 병존
  (**관측됨**).
- **왜 중요한가**: 학생/교사 모두에게 "복습"이라는 개념이 4가지 다른
  방식으로 흩어져 있어 일관성이 없고, C1(간격 반복)이 배선되면 5번째
  동선이 추가되는 셈이라 통합이 선행되지 않으면 혼란이 커짐.
- **제안 해법**: C1 배선 시점에 맞춰 4개 동선을 하나의 "돌아오는 단어"
  개념으로 재편(기존 개별 규칙을 없애는 것이 아니라 진입점을 통합).
- **난이도**: L(4개 기존 화면/로직에 걸침). **리스크**: 중(기존 플로우
  다수 접점). **우선순위**: 중(C1 이후로 순서 배치 권장).
- **검증**: 통합 전후 복습 진입률/완료율 비교(C2 계측 선행 필요).

### H4. 세션 길이 정직성 · 마이크로 세션

- **문제**: 세션 크기가 배정량에 따라 5~20단어, 종합 모드 단어당
  2~4단계로 가변적(**관측됨**) — 학생이 시작 전 소요 시간을 가늠하기
  어려울 수 있음(**추정**).
- **왜 중요한가**: 8살 아동은 "짧고 확실한 끝"에 대한 예측 가능성이
  지속 참여에 중요(Duolingo류 짧은 세션 원리, §참고제품).
- **제안 해법**: 세션 시작 전 "오늘은 O단어, 약 O분"류의 예측 가능한
  안내 표시(새 세션 타입을 만들지 않고 기존 배정량 정보를 노출).
- **난이도**: S. **리스크**: 낮음. **우선순위**: 중.
- **검증**: 표시 전후 세션 완주율 비교(C2 계측 필요).

### H5. 티켓 콜드스타트

- **문제**: 티켓 17건 지급(9명)에 소비 0건. 상점 최저가 8장=8일
  (**관측됨**).
- **왜 중요한가**: "모아도 쓸 데가 없다"는 체감이 동기를 꺾을 수 있음.
- **제안 해법**: **가챠 확장 없이**(제품 불변 조항 준수) 상점 가격
  구조나 티켓 발생 경로 재검토(예: 4/4 완료 외에 소량의 추가 결정론적
  발생원 — 단, "새 화폐 금지"는 유지, 기존 티켓 체계 안에서만).
  구체 대안은 `GAME_SYSTEM_IDEAS.md` 참고.
- **난이도**: S~M. **리스크**: 중(경제 변경, 운영자 승인 필요).
  **우선순위**: 중.
- **검증**: 가격/발생원 조정 전후 소비 전환율(지급 대비 소비 건수)
  비교 — 지금 데이터(소비 0건)로 이미 문제는 확인됨, 개선 효과만
  추가 계측 필요.

### H6. 스트릭 마일스톤 재획득 가능화 (버그성)

- **문제**: `milestoneStreak`이 high-water mark라 30일 달성 후 스트릭이
  끊기면 재달성해도 3/7/14/30일 스티커를 다시 받을 수 없음(**관측됨**,
  `useStudent.js:1126`).
- **왜 중요한가**: `GAME_DESIGN.md` §12가 "실제 손실을 주는 타이머
  없음... 스트릭이 끊겨도 별/스티커/모자 단계는 절대 삭제되지 않는다"고
  명시하는데, 이 서술은 "이미 받은 것을 뺏지 않는다"는 뜻으로는 코드와
  일치하지만(**관측됨**, 실제로 삭제 로직 없음), "다시 노력하면 다시
  보상받을 수 있다"는 습관 형성의 재참여 유인 관점에서는 현재 구조가
  한 번의 30일 달성 이후 스트릭 재도전의 보상 축을 영구히 소진시킨다
  (**관측됨**, 코드 동작) — 문서의 "손실 없음" 서술이 함의하는 기대와
  실제 재참여 유인 사이의 간극은 **추정**으로 표기.
- **제안 해법**: `milestoneStreak`을 절대 최고치가 아니라 "현재
  스트릭이 끊긴 뒤 재도전 사이클"의 상태로 재설계(이미 받은 뱃지를
  회수하지 않으면서, 새 스트릭 사이클에서 3/7/14/30을 다시 축하할 수
  있게). 스트릭 징벌 금지 조항과 무관 — 오히려 재참여 보상을 늘리는
  방향.
- **난이도**: S(카운터 로직 변경). **리스크**: 낮음-중(기존 학생의
  이미 받은 스티커는 그대로 — 회귀 위험 낮음). **우선순위**: 상
  (버그성이라 임팩트 대비 난이도가 낮음).
- **검증**: 즉시 검증 가능 — 수정 후 스트릭 재도전 시나리오에서
  마일스톤 스티커 재지급 여부 확인.

### H7. 1000⭐ 배지 영구 소실 (버그)

- **문제**: `STAR_BADGES`의 1000점 항목이 지급 대상 스티커 id `lion`을
  참조하는데, `src/data/stickers.js`의 `STICKERS` 배열에 `lion` id가
  존재하지 않는다(**관측됨**, 직접 코드 조회로 확인 —
  `useStudent.js:158`, `stickers.js` 전수). 임계값(`starBadgeThreshold`)은
  갱신되므로 재시도도 불가능 — 1000⭐를 달성한 학생은 아무것도 받지
  못하고 그 사실조차 재확인 불가.
- **왜 중요한가**: 가장 상위 마일스톤에서 정확히 "아무 일도 일어나지
  않는" 조용한 실패 — 발견하기 가장 어려운 종류의 버그.
- **제안 해법**: `STAR_BADGES`의 `stickerId: 'lion'`을 `STICKERS`에
  실재하는 legendary id로 교체(신규 스티커 자산 추가가 아니라 기존
  오타 수정 성격).
- **난이도**: S(상수 1줄). **리스크**: 낮음. **우선순위**: 상(즉시
  수정 가능한 명백한 버그).
- **검증**: 즉시 검증 가능 — 수정 후 1000⭐ 도달 시 스티커 지급 여부
  확인.

---

## Medium

- **M-a. 미니게임 보상 상한 재점검** — 라운드당 별 10, 세션 최대 50,
  재플레이 무제한 구조가 학습 없이도 보상 총량을 키울 수 있는지 검토
  (**관측됨**, 규칙). 난이도 S, 리스크 중(경제 변경), 검증: 현재
  데이터로 미니게임 보상 비중 자체는 확인 가능하나(xp_ledger에 명시적
  구분 없음, 별 지급 로그 별도 확인 필요 — **미확인**).
- **M-b. 스티커 다이어리 발견성 개선** — 스티커 총 250개, 중앙값 0
  (**관측됨**)이 배치 UI 저사용 때문인지 스티커 자체 무관심인지
  구분 안 됨(**미확인**). 난이도 S, 리스크 낮음.
- **M-c. 관리자 검수함 배치 처리 확대** — `SpellingReviewQueuePanel`
  등 검수 큐가 개별 항목 위주(**관측됨**, `NEXT_PRIORITY.md`). 학생
  경험과 무관, 교사 생산성 항목. 난이도 M, 리스크 낮음.
- **M-d. 다중 반 동시 배정 표시 개선** — 여러 반 소속 학생의 배정/
  진행 표시가 단일 반 가정에 가까움(**관측됨**, `NEXT_PRIORITY.md`,
  학생 식별은 이미 UUID 기준으로 정확 — 규칙 4 위반 아님). 난이도 M,
  리스크 낮음.
- **M-e. 레벨업 미션 3탭 루프 재설계** — 반복 탭 유도 구조 체감 개선
  (**관측됨**, `NEXT_PRIORITY.md`). 난이도 M, 리스크 낮음.
- **M-f. 렌더 스모크 테스트 도입** — 학생 경험 항목은 아니나, 28차에서
  `adminPin` 미배선 ReferenceError가 build/verify 둘 다 못 잡은 검증
  공백이 실증됨(**관측됨**, `handoff.md` 28차 2절). 난이도 M, 리스크
  낮음(테스트 인프라 항목).

## Low

- **L-a. 발음 재생 지연 표시 추가 개선 여지** — mp3 부재 시 TTS 폴백
  1~3초 지연에 대한 "재생 중" 표시는 28차에 이미 보완됨(**관측됨**),
  추가 개선 여지(로딩 애니메이션 등)는 저우선.
- **L-b. 콤보 시각 효과 강화** — 쓰기 콤보(3/5/10) 보너스는 이미
  있으나 시각적 강조가 약할 가능성(**추정**, 화면 렌더 미확인).
- **L-c. 숙제 2라운드 문구 유지** — "2라운드 시작 시 문구가 사라진다"는
  현상(**관측됨**, 작업 지시문 인용) — 저위험 표시 버그.

---

## 부록: ROI Top 50

컬럼: 순위 / 항목 / 분류 / 기대효과(1~5) / 개발난이도(1~5, 낮을수록
쉬움) / 리스크(1~5) / **ROI = 기대효과×2 − 난이도 − 리스크** / 우선순위.

_난이도 환산: S≈1~2, M≈3, L≈4~5. 이 표의 항목은 위 본문(Critical/High/
Medium/Low)과 `NEXT_PRIORITY.md`/`BUG_REPORT.md`에 이미 실존 기록된
항목만 사용했다 — 실질 후보 44개를 확보했고, 억지로 50개를 채우기 위한
가상 항목은 추가하지 않았다(정직 기록 원칙, 규칙 18)._

| 순위 | 항목 | 분류 | 기대효과 | 난이도 | 리스크 | ROI | 우선순위 |
|---|---|---|---|---|---|---|---|
| 1 | H7. 1000⭐ 배지 lion id 오타 수정 | 버그 | 4 | 1 | 1 | 6 | 최상 |
| 2 | C5. 숙제 원클릭 배정 동선 노출 | 교사UX | 5 | 1 | 1 | 8 | 최상 |
| 3 | C3. 4칸 진행 상시 표시 | 학생UX | 5 | 1 | 1 | 8 | 최상 |
| 4 | H6. 스트릭 마일스톤 재획득 가능화 | 버그 | 4 | 2 | 1 | 5 | 최상 |
| 5 | C2. 핵심 학습 플로우 계측 | 학습효과 | 5 | 3 | 1 | 6 | 최상 |
| 6 | H4. 세션 길이 사전 안내 | 학생UX | 3 | 1 | 1 | 4 | 상 |
| 7 | M9(BUG_REPORT). entrance_tests anon 쓰기 개방 | 버그 | 3 | 2 | 2 | 2 | 상 |
| 8 | C1. 간격 반복(Leitner) 배선 | 학습효과 | 5 | 3 | 2 | 5 | 최상 |
| 9 | H2. "알아요" 1문항 확인 후 스킵 | 학습효과 | 4 | 2 | 2 | 4 | 상 |
| 10 | 보안1(NEXT_PRIORITY). PIN ilike 이스케이프 브랜치 머지 | 버그 | 4 | 1 | 1 | 6 | 최상 |
| 11 | H5. 티켓 콜드스타트(가격/발생원 재검토) | 학생UX | 3 | 2 | 3 | 1 | 상 |
| 12 | 보안4(H4). 라운드 완료 signature 충돌 수정 | 버그 | 4 | 3 | 3 | 2 | 상 |
| 13 | 보안3(H3). accepted_meanings 배치 lost-update | 버그 | 3 | 3 | 3 | 0 | 상 |
| 14 | 보안2(H2). admin-content-write 검증 순서 교정 | 버그 | 4 | 3 | 3 | 2 | 상 |
| 15 | C4. 퀴즈 재인→회상 난이도 보정 | 학습효과 | 4 | 3 | 3 | 2 | 중상 |
| 16 | M6. spellingReviewQueue 삭제 단어 id 잔존 | 버그 | 2 | 1 | 1 | 2 | 중 |
| 17 | M12. generate-audio PATCH 인코딩 비대칭 | 버그 | 2 | 1 | 1 | 2 | 중 |
| 18 | M10. self-set/set-student-pin 레이스 가드 머지 | 버그 | 3 | 1 | 1 | 4 | 상 |
| 19 | 메모리 엔진 배선(review_data read-merge-write) | 학습효과 | 5 | 3 | 3 | 3 | 상(C1 선행) |
| 20 | M-f. 렌더 스모크 테스트 도입 | 성능/품질 | 3 | 3 | 1 | 2 | 중 |
| 21 | H1. 강제 녹음 게이트 완화(스킵 옵션) | 학생UX | 4 | 3 | 3 | 2 | 상 |
| 22 | M-e. 레벨업 미션 3탭 루프 재설계 | 학생UX | 3 | 3 | 2 | 1 | 중 |
| 23 | M-c. 검수함 배치 처리 확대 | 교사UX | 3 | 3 | 1 | 2 | 중 |
| 24 | M-d. 다중 반 동시 배정 표시 개선 | 교사UX | 3 | 3 | 2 | 1 | 중 |
| 25 | M3. SpellingQuestion 타이머 언마운트 미정리 | 버그 | 2 | 1 | 1 | 2 | 중 |
| 26 | M4. SpeechBtn 늦은 콜백 과다 지급 | 버그 | 2 | 2 | 2 | 0 | 중하 |
| 27 | M5. 빈 로컬 복원이 병합 아닌 교체 | 버그 | 3 | 2 | 2 | 2 | 중 |
| 28 | M8. wordLibrary 컬럼 폴백이 모든 에러에서 발동 | 버그 | 2 | 2 | 2 | 0 | 중하 |
| 29 | M7. 오토파일럿 UI 미잠금(동시 쓰기) | 버그 | 2 | 2 | 1 | 1 | 중하(플래그 off) |
| 30 | M11. updateTextbookMeta/deletePublisher 듀얼패스 미배선 | 버그 | 3 | 3 | 3 | 0 | 상(락다운 선행조건) |
| 31 | 차기 락다운 배치(9테이블) | 성능/보안 | 3 | 5 | 4 | -3 | 중(단계적) |
| 32 | H3. 복습 동선 4종 통합 | 학습효과 | 3 | 4 | 3 | -1 | 중(C1 이후) |
| 33 | C6. 보상-학습 정렬(XP 재배분) | 학습효과 | 4 | 2 | 4 | 2 | 중(승인 필요) |
| 34 | M-a. 미니게임 보상 상한 재점검 | 학생UX | 2 | 1 | 3 | 0 | 중하 |
| 35 | M-b. 스티커 다이어리 발견성 개선 | 학생UX | 2 | 2 | 1 | 1 | 중 |
| 36 | L-c. 숙제 2라운드 문구 유지 | 버그 | 1 | 1 | 1 | 0 | 하 |
| 37 | L-a. 발음 재생 로딩 애니메이션 | 학생UX | 1 | 1 | 1 | 0 | 하 |
| 38 | L-b. 콤보 시각 효과 강화 | 학생UX | 1 | 1 | 1 | 0 | 하 |
| 39 | GAME_SYSTEM_IDEAS 하우스 점수 정체 완화 | 학생UX | 3 | 3 | 3 | -1 | 중(경제 변경) |
| 40 | GAME_SYSTEM_IDEAS 모자(hat) 도달률 개선 | 학생UX | 3 | 3 | 2 | 0 | 중 |
| 41 | GAME_SYSTEM_IDEAS 정원/성장 서사 확장안 1 | 학생UX | 3 | 4 | 3 | -1 | 중하(승인 필요) |
| 42 | GAME_SYSTEM_IDEAS 정원/성장 서사 확장안 2 | 학생UX | 3 | 4 | 3 | -1 | 중하(승인 필요) |
| 43 | 07-24 이후 사용 급감 원인 운영자 확인 | 학습효과 | 5 | 1 | 1 | 8 | 최상(조사, 개발 아님) |
| 44 | word_status 계측 세분화(스킵 지점 저장) | 학습효과 | 4 | 2 | 2 | 4 | 상(C2와 병행) |

_실질 후보 44개. 항목 43("사용 급감 원인 확인")은 개발 항목이 아니라
운영자 확인 요청이라 "개발난이도" 축이 다른 항목과 의미가 다르지만,
ROI 관점에서 조사 비용이 극히 낮고 다른 모든 판단(특히 07-24 이후
감소가 제품 문제인지 아닌지)의 선행 정보라 표에 포함했다._

### 상위 10개 근거

1. **H7(1000⭐ 배지 오타)** — 상수 1줄 수정으로 영구 소실 버그를
   완전히 해소, 리스크 사실상 0.
2. **C5(숙제 원클릭 배정)** — 이미 구현된 자동 생성 로직을 노출만
   하면 되는 최저 난이도·최대 임팩트 조합.
3. **C3(4칸 진행 표시)** — 기존 데이터(round 카운터)만으로 구현
   가능, 신규 로직 없음.
4. **H6(스트릭 마일스톤 재획득)** — 카운터 로직 변경 수준, 기존
   지급분 영향 없음.
5. **C2(핵심 플로우 계측)** — 난이도는 중간이지만 이후 모든 항목의
   ROI 판단 근거가 되는 승수 효과.
6. **H4(세션 길이 안내)** — 표시 전용, 리스크 최소.
7. **M9(entrance_tests anon 쓰기)** — 이미 감사에서 확인된 실존 취약점,
   blast radius가 좁아 리스크는 낮게 평가되나 방치 시 신뢰도 문제.
8. **C1(간격 반복 배선)** — 임팩트 최상급이나 저장 계층 선행 수정이
   필요해 순위가 5위 밑으로 내려감.
9. **H2("알아요" 확인 스킵)** — 자기평가 정확도 개선, 기존 UX 톤 유지.
10. **보안1(PIN 이스케이프 브랜치 머지)** — 이미 코드 작성·빌드 검증
    완료, 운영자 승인만 남아 실질 구현 비용이 거의 0.
