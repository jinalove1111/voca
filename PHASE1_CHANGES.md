# Phase 1 UX 개선 — 완료 기록 (2026-08-03)

`LEARNING_UX_AUDIT.md`(2026-08-02 감사, `dac9c6e`)의 "1단계 — 거짓말부터
없앤다" 로드맵을 실제 코드로 반영한 세션의 기록이다. 대상은 화면이 사실과
다른 말을 하거나, 정보가 있는데 안 보여주거나, 버튼 라벨과 실제 동작이
어긋난 저위험 항목뿐 — 새 학습 로직/새 보상/새 화면은 이번 범위에 없다
(CLAUDE.md 규칙 12 준수).

## 1. Before/After 대조표

### 로그인(StudentSelect.jsx) — `fface60`

| 화면/상황 | Before | After |
|---|---|---|
| 로그인 실패 — 이름을 못 찾음(`not_found`) | "해당 이름의 학생을 찾을 수 없어요. 처음이면 아래 \"처음이에요\" 탭에서 등록해주세요." (아래를 가리키지만 실제 탭은 위쪽) | "이름 철자를 확인해보고, 선생님이 등록해줬다면 위 \"PIN 만들기\" 탭을 눌러보세요." |
| 로그인 실패 — PIN 미설정(`no_pin_setup`) | "아직 PIN이 설정되지 않은 계정이에요. 선생님(관리자)에게 문의해주세요." (실제로는 학생 스스로 PIN 만들기 탭에서 해결 가능한데 관리자에게 미루는 안내) | "위 \"PIN 만들기\" 탭에서 네 이름을 찾아 PIN을 만들어봐요." |
| "처음이에요" 등록 탭 — 반 목록이 비어있을 때 | 반 선택 `<select>`가 그냥 사라지고 등록 버튼은 계속 활성 상태(눌러도 반이 없어 실패) | "지금은 등록할 수 없어요 — 선생님께 말해주세요." 안내 표시 + 등록 버튼 비활성화 |
| PIN 설정 완료 후 | 축하 화면에서 "바로 시작하기!" 버튼을 학생이 직접 한 번 더 눌러야 진입 | 축하 문구를 1.5초 보여준 뒤 자동으로 진입(버튼은 더 빨리 넘어가고 싶을 때를 위한 폴백으로 유지) |

### 발음/퀴즈 판정 피드백(WordDetail.jsx, QuizGame.jsx) — `ae60fc5`

| 화면/상황 | Before | After |
|---|---|---|
| WordDetail 발음(SpeechBtn) — STT 미채점(녹음만 성공) | "✅ 발음 성공!" (정확도 판정을 실제로 한 것처럼 단정) | "✅ 녹음 완음!" — 실제로는 `녹음 완료!` 계열 문구로 통일(정확도 미판정임을 정직하게 표현) |
| WordDetail PronounceStep — 발음만 다시 듣는 버튼 | "🔁 다시 공부" (실제 동작은 재생뿐인데 "공부"라는 넓은 표현) | "🔁 다시 듣기" |
| WordDetail QuizStep — 오답 후 "다음" 버튼 | 정답이든 오답이든 항상 초록색 "✅ 완료! 다음 단어 →" (오답도 성공처럼 보임) | 오답이면 회색/보라 중립 스타일 "다음 단어 →"로 낮춤(정답일 때만 초록 강조 유지) |
| QuizGame(홈 퀴즈) PronStep — 침묵 2초여도 녹음만 되면 | "발음 성공! ⭐ 1개 획득!" / 버튼 "✅ 발음 성공!" | "녹음 완료! ⭐ 1개 획득!" / 버튼 "✅ 녹음 완료!" (별 지급 조건 `blob.size>0`은 무변경, 문구만 정직화) |

### 홈(Dashboard.jsx, EntranceTestBanner.jsx, paulTown.js) — `f19edef`

| 화면/상황 | Before | After |
|---|---|---|
| 첫 방문(학습 기록 0건) 홈 히어로 | "오늘도 시작해볼까요?" (100일차 학생과 동일 문구, "오늘도"라는 반복 전제) | "🎉 첫 학습을 시작해볼까요? — 폴이 옆에서 같이 할게요!" (기존 문구는 첫 방문 아닐 때 그대로 유지) |
| 정원 빈 상태(꽃 0송이·오늘 심은 별 0개) | "🌷 꽃 0송이 · 오늘 심은 별 0개 · {단계명}" (0을 그대로 노출) | "🌱 첫 별을 심으면 정원이 시작돼요" |
| 미션 카드(4칸) 하단 안내 | "4개를 모두 완료하면 🎁 선물상자! ..." 한 줄만 | 위에 "👆 위의 '오늘의 학습'을 시작하면 4개가 같이 채워져요" 한 줄 추가(기존 문구는 그대로 유지) |
| "🎖️ 내 기록 더보기" details 서브라벨 | 제목만 표시 | 제목 아래 "⭐ 랭킹 · 🎫 티켓 상점 · 📊 누적 통계" 서브라벨 추가 |
| "🧭 더 많은 메뉴" details 서브라벨 | 제목만 표시 | 제목 아래 "🎮 게임 · 🎩 모자 · 🏛️ 박물관 · 📅 캘린더" 서브라벨 추가 |
| 입실시험 배너 부제 | "지금 바로 참여하세요" (문제 수/제한 시간 정보 없음) | 가능하면 "N문제 · 약 M분이면 끝나요"(문제 수/시간 정보가 있을 때), 둘 다 없으면 기존 문구 그대로 폴백 |

### 복원 대기 화면 / 마이크 권한(App.jsx, Dashboard.jsx MicPrimeBtn) — `75dd05a`

| 화면/상황 | Before | After |
|---|---|---|
| 로그인 직후 복원 대기 화면(이 화면이 보이는 학생은 정의상 로컬 기록이 항상 빈 상태) | "학습 기록을 불러오는 중..." (로컬에 뭔가 있어서 읽는 중이라는 오해) | "폴이 네 자리를 준비하고 있어요..." (로컬 기록이 비어 있을 때만 분기, 클라우드 복원 기록이 있으면 기존 문구 유지) + 로그인한 학생 이름 한 줄 표시(동명이인 오로그인 확인용) |
| 마이크 권한 요청 시점 | 로그인 화면 첫 터치(touchstart/pointerdown)에서 `getMicStream()` 자동 호출 — 학습을 시작하기도 전에 이유 설명 없는 OS 팝업 | 로그인 화면에서는 요청하지 않음(unlockAudio/primeSpeech만 유지) — 홈의 기존 마이크 배너 버튼(MicPrimeBtn)을 눌렀을 때만 요청(설명 문구가 있는 자리) |
| MicPrimeBtn — 권한 허용 직후 | `state === 'ready'`가 되는 순간 배너가 소리 없이 사라짐(방금 누른 버튼이 뭘 했는지 확인 불가) | "✅ 마이크 준비 완료!"를 1.5초간 같은 자리에 보여준 뒤 사라짐 |
| MicPrimeBtn — 요청 전 버튼 위 안내 | 버튼만 있고 왜 필요한지 설명 없음 | "따라 말하기에서 네 목소리를 듣고 칭찬해주려면 필요해요" 한 줄 추가 |

### 발음 판정 문구(재확인) + "모르겠어요" — `048d3c1` + 회귀 원복 `ad55680`

| 화면/상황 | Before(048d3c1 이전) | 048d3c1(회귀 발생) | After(ad55680, 최종) |
|---|---|---|---|
| PronounceStep "모르겠어요" 버튼 | 색만 바뀌고 별다른 반응 없음(상태 저장만) | 색이 바뀌고 발음 자동 재생 + 암기 팁 카드가 **버튼을 눌러야만** 펼쳐짐(있던 정보가 오히려 숨겨짐 — 버튼을 안 누르면 이전보다 정보가 줄어듦) | 발음 자동 재생은 유지, 암기 팁 카드는 **항상 노출**(원래 동작 그대로) — 버튼을 누르지 않아도 정보 손실 없음 |

### 쓰기 시험 en2kr 힌트(SpellingQuestion.jsx) — `df0e51e`

| 화면/상황 | Before | After |
|---|---|---|
| 3회 오답 후 잠금 해제 보상 — en2kr(영어 제시 → 한글 뜻 입력) 방향 | "탭하면 발음을 들려줘요" 버튼(=이미 화면에 큰 글씨로 보이는 영어 단어를 다시 읽어줄 뿐, 정보량 0) | "💡 뜻 힌트" + 첫 글자 힌트 표시(kr2en 방향은 기존 발음 재생 그대로 유지, 채점 로직 무변경) |

### 세션 완료 카드(GuidedSession.jsx) — `44ed5f6`

| 화면/상황 | Before | After |
|---|---|---|
| 완료 카드 — 이번 세션 성적 | 표시 없음(단어 완료 개수만) | "이번 세션 퀴즈 {correct}/{answered}" + 재시도했던 단어 목록("다시 푼 단어: ...") 추가(새 집계 아님, 이미 계산돼 있던 값 재사용) |
| 완료 카드 — 버튼 위계(아직 더 할 단어가 남은 경우) | "🏠 오늘은 여기까지"가 강조색(그라데이션), "▶ 다음 세션 계속하기"가 보조 스타일(실제로 이어가야 할 행동이 덜 강조됨) | 강조 스타일을 서로 교체 — "계속하기"가 강조, "여기까지"가 중립(더 할 단어가 없으면 기존처럼 "여기까지"가 강조 유지). 문구/순서/로직은 무변경 |
| 완료 카드 — 내일 예고 문구 | "내일 복습할 단어 {reviewQueueCount}개가 기다리고 있어요"(이 숫자는 이월된 복습 대기열만 세고, 오늘 이 세션에서 새로 틀린 단어는 포함하지 않음 — 부정확한 숫자 주장) | "틀린 단어는 내일 다시 만나요"(숫자 주장 제거, 존재 여부만 기존 prop으로 판단) |

## 2. 변경 파일 목록과 커밋 해시

| 커밋 | 제목 | 변경 파일 |
|---|---|---|
| `fface60` | fix(login): 실패 안내가 올바른 탭을 가리키도록 정정 + PIN 설정 후 자동 진입 | `src/components/StudentSelect.jsx` |
| `ae60fc5` | fix(feedback): 녹음 판정 문구 통일·버튼 라벨과 동작 일치·오답 후 중립 표기 | `src/components/QuizGame.jsx`, `src/components/WordDetail.jsx` |
| `f19edef` | polish(home): 첫날 환영 문구·정원 빈 상태·미션 안내·메뉴 라벨·시험 배너 정보 | `src/components/Dashboard.jsx`, `src/components/EntranceTestBanner.jsx`, `src/utils/attachment/paulTown.js` |
| `048d3c1` | feat(study): '모르겠어요'가 발음 재생·암기 팁으로 실제 도움 제공(→ 회귀, `ad55680`에서 원복) | `src/components/WordDetail.jsx` |
| `df0e51e` | feat(writing): 방향별 힌트 정합 — en2kr 3회 오답 잠금 해제 보상을 첫 글자 힌트로 | `src/components/SpellingQuestion.jsx` |
| `75dd05a` | polish(app): 첫 방문 대기 문구·마이크 권한 시점 정리·준비 완료 피드백 | `src/App.jsx`, `src/components/Dashboard.jsx` |
| `44ed5f6` | polish(session): 완료 카드 성적 표시·계속하기 위계·내일 예고 문구 정직화 | `src/components/GuidedSession.jsx` |
| `c4acb4c` | chore(ai-status): UX 웨이브 2(학습 루프 정직성) 완료 체크포인트 | `.ai-status/*.json` |
| `ad55680` | fix(study): 암기 팁 상시 노출 원복 — 도움 추가가 기존 정보를 가리지 않도록 | `src/components/WordDetail.jsx` |

## 3. 검증 결과

각 커밋마다 `npm run build` + 해당 도메인 `npm run verify:<domain>`을
실행해 PASS 확인 후 진행했다. 세션 마감 시점(`ad55680` 이후) 전체 재검증:

- `npm run build` — 통과(에러 0, 213 modules transformed, 신규 경고 없음)
- `npm run verify:all` — 아래 세트로 확인(오늘 커밋들로 인한 회귀 없음, 전부
  환경/기존 제약):

  **FAIL**
  - `login` — `testStudentSelectPinStatus.mjs`/`testStudentPinAuth.mjs`/
    `testStudentPinSelfSetup.mjs`/`testClearStudentPin.mjs`가 로컬에
    `SUPABASE_SERVICE_ROLE_KEY`가 없어 `permission denied for table
    students` 등으로 실패(같은 실행의 `testXpLedgerDb.mjs`가 명시하듯
    "SUPABASE_SERVICE_ROLE_KEY가 로컬에 없음(.env.local)"이 원인 — 서비스
    롤 키가 있는 Vercel 프로덕션에서는 정상 동작 예상). 오늘 Phase 1
    커밋 중 이 4개 스크립트가 다루는 PIN 인증 관련 코드(`api/*.js`)는
    아무것도 건드리지 않았다 — 회귀 아님, 환경 요인.

  **SKIP**
  - `speaking`(말하기 — 녹음 후 따라 말하기), `listening`(듣기 — 발음
    재생) — 하네스 설계상 항상 SKIP(실제 오디오 재생/녹음은 headless
    환경에서 검증 불가).
  - `student`/`admin`/`homework`/`wordAssignment` 도메인 내 다수
    스크립트가 "락다운 환경 — anon 쓰기 불가, admin-content-write pin
    경로로 재검증 필요"로 SKIP(v3.11 RLS 락다운 관련, 기존부터 있던
    제약 — 오늘 변경 없음).
  - `paulRank`/`wordKing`/`examples` 등 일부 라이브 e2e 스크립트도 테이블
    미적용/서비스롤 키 부재로 SKIP(기존부터 있던 상태).

  **PASS**
  - `student`, `admin`, `homework`, `quiz`, `writing`, `unitSwitching`,
    `persistence`, `dailyStudy`, `wordAssignment`, `audioTts`, `paulRank`,
    `ticketEconomy`, `wordKing`, `houseSystem`, `seasonalProgression`,
    `dailyRitual`, `attachment`, `analytics`, `reading`,
    `sentenceLearning`, `examples`, `learningEngine`, `memoryEngine`
    — 전 도메인 요약 PASS(각 도메인 내부에 위에 적은 환경/락다운 SKIP
    라인이 포함되어 있어도, 도메인 전체 판정은 PASS).

  이 결과는 세션 시작 전 기대 베이스라인("FAIL {login}(환경) + SKIP
  {speaking, listening} + v3.11 락다운 SKIP 라인들 + 나머지 전 도메인
  PASS")과 정확히 일치한다 — 회귀 없음.

## 4. 의도적으로 하지 않은 것

- **2회 오답 강제 힌트 노출**(`LEARNING_UX_AUDIT.md` B급/F3) — SKIP.
  `hintEnabled`는 교사가 반/시험 단위로 힌트 허용 여부를 끄고 켜는
  설정인데, 그 설정과 무관하게 2회 오답 시점부터 힌트를 강제로 보여주면
  `hintEnabled=false`로 의도적으로 힌트를 막아둔 교사의 설정을 우회하게
  된다 — 설정 의미와 직접 충돌 위험이 있어 지시대로 SKIP(`df0e51e` 커밋
  메시지에 동일 사유 기재).
- **Phase 2·3 전체** — 아래 5절/6절 참고. 학습 흐름·보상 배선을 바꾸는
  항목은 전부 운영자 승인이 필요해 이번 세션 범위에 포함하지 않았다.

## 5. Phase 2 잔여 항목 (운영자 승인 필요)

`LEARNING_UX_AUDIT.md` 6장 "2단계 — 성장을 되살린다" 대상. `cleared`의
정의(레벨업 미션 3연속 정답 클리어) 자체는 절대 바꾸지 않고, 그 값이
"채워지는 경로"만 넓히는 방향:

- **S1 — 진도·완주 판정의 파생 집합 확장**: 진도 바/유닛 완주/모자/마을이
  전부 `cleared` 배열 하나(레벨업 미션 3연속 정답)에만 의존해, GuidedSession
  만으로 학습하는 학생은 진도가 거의 늘지 않는 문제(`LEARNING_UX_AUDIT.md`
  2장). `cleared` 정의는 불변, 파생 집합만 확장.
- **S2 — GuidedSession 오답의 미션 승격**: GuidedSession 안에서 틀린 문제가
  레벨업 미션으로 이어지지 않는 문제.
- **S5 — 홈에 복습(spellingReview) 진입점**: 복습 대기열로 바로 이동하는
  버튼이 홈에 상시 노출되지 않음.
- **홈 정원 지표와 정원 화면 지표 일치**: `paulTown.js`의 `gardenBandSummary`
  홈 배너 표시와 정원 화면 자체의 성장 지표가 서로 다른 값을 참조하는
  불일치(이번 세션에서는 "0송이"일 때 문구만 정직화했고, 지표 자체의
  일치는 범위 밖).
- **모자 1종 획득 조건 정정**: `masteredCount`가 학생 경로에서 구조적으로
  영구 0에 가까워 그 값을 임계값으로 쓰는 모자 1종이 사실상 영구 잠김
  (`LEARNING_UX_AUDIT.md` A급 7번, `hatSystem.js`).
- **`GOAL=5` 클램프**: `useStudent.js`의 `const GOAL = 5`가 고정값이라
  4개 미션 목표량을 조정할 수 없는 문제(`LEARNING_UX_AUDIT.md` A급 14번).
- **`useAttachment` 판정 즉시 반영**: 판정이 로그인당 1회만 돌아 세션 도중
  모자/밀스톤 임계값을 넘겨도 그 순간엔 축하가 없는 문제(`useAttachment.js`,
  `LEARNING_UX_AUDIT.md` A급 15번).

## 6. Phase 3 잔여 항목 (운영자 결정)

`LEARNING_UX_AUDIT.md` 6장 "3단계 — 기억을 만든다" 대상. 학습과학/보상
경제/비용(STT 등) 트레이드오프가 얽혀 있어 전부 운영자 결정 필요:

- **녹음 게이트 완화**: 부끄러워하는 학생의 녹음 회피 경로와 그로 인한
  학습적 대가(`LEARNING_UX_AUDIT.md` 4.6절).
- **회상 인출 비중 확대**(쓰기 기본값 on/off 재검토, S4).
- **Leitner 배선**(간격 반복 로직이 있다면 그 배선 확인) — 단, `review_data`
  덮어쓰기 이슈를 먼저 조사·수정하는 게 선행 과제.
- **미션(레벨업) 클리어에 최소 간격 조건 추가**.
- **보상 재정렬**(미니게임 상한/중복 스티커/퀴즈 정답 보상, S3) — 새 XP
  발생원/곱연산 배수/무작위 보상/가챠는 여전히 전부 금지(7장 제약 불변).

## 7. 운영자 확인 경로

`npm run dev` 실행 후 학생으로 로그인해 아래 화면을 직접 열어보면 이번
세션의 변경을 확인할 수 있다. **자동 스크린샷은 만들지 않았다** — 실제
프로덕션 학생 데이터가 화면에 표시되므로, 스크린샷 파일 자체가 개인정보를
담게 되는 걸 피하기 위함이다.

1. **로그인 화면** — 등록 안 된 이름/PIN 미설정 계정으로 로그인 시도 →
   실패 안내 문구가 "PIN 만들기" 탭을 정확히 가리키는지 확인. "처음이에요"
   탭에서 반 목록이 없으면(정상 케이스가 아니라면 스킵) 등록 버튼이
   비활성화되고 안내가 뜨는지 확인. PIN 설정을 새로 완료해보고 자동으로
   홈까지 진입하는지 확인.
2. **로그인 직후 대기 화면** — 새 학생(또는 로컬 저장소를 비운 기존 학생)
   으로 로그인해 "폴이 네 자리를 준비하고 있어요..." 문구와 로그인한
   이름이 뜨는지 잠깐 확인(순식간에 지나갈 수 있음).
3. **홈 화면** — 학습 기록이 없는 신규 학생이면 히어로 카드 문구가
   "첫 학습을 시작해볼까요?"인지, 정원 밴드가 "첫 별을 심으면 정원이
   시작돼요"인지 확인. 미션 카드 하단에 안내 한 줄이 추가됐는지, "내 기록
   더보기"/"더 많은 메뉴" 아코디언 제목 아래 서브라벨이 보이는지 확인.
   입실시험이 켜진 반이면 배너 부제에 문제 수/시간이 뜨는지 확인.
   마이크 배너를 눌러 권한을 허용해보고 "✅ 마이크 준비 완료!"가 1.5초간
   보이는지 확인.
4. **단어 학습(공부하기/종합 모드)** — 단어 카드에서 "😅 모르겠어요"를
   눌러 발음이 자동 재생되는지, 암기 팁이 있는 단어라면 버튼을 누르지
   않아도 암기 팁 카드가 처음부터 보이는지 확인. "🔁 다시 듣기" 라벨을
   확인. 발음 녹음 후 STT 미채점 상태면 "✅ 녹음 완료!" 문구가 뜨는지
   확인. 퀴즈에서 일부러 틀려보고 "다음 단어 →" 버튼이 중립(회색/보라)
   스타일인지 확인.
5. **홈 퀴즈(QuizGame)** — 발음 단계에서 침묵만 녹음해보고 "녹음 완료!"
   문구가 뜨는지 확인.
6. **쓰기 시험(en2kr 방향, 켜져 있는 반만)** — 같은 단어를 3번 틀려
   잠금이 풀리는 지점에서 "💡 뜻 힌트"와 첫 글자가 뜨는지 확인(kr2en
   방향은 기존처럼 발음 재생 버튼이 뜨는지 대조).
7. **3분 데일리 리추얼(GuidedSession) 완료 카드** — 세션을 하나 끝내고
   "이번 세션 퀴즈 N/M"과(재시도한 단어가 있다면) "다시 푼 단어: ..."가
   보이는지, 아직 더 할 단어가 남아있을 때 "▶ 다음 세션 계속하기"가
   강조 스타일(보라/남색 그라데이션)이고 "🏠 오늘은 여기까지"가 중립
   스타일인지, 더 할 단어가 없으면 반대(여기까지가 강조)인지 확인.
   "틀린 단어는 내일 다시 만나요" 문구도 확인.
