# 학습 UX 감사 — 첫 로그인부터 유닛 완주까지

_작성일 2026-08-03. 방학 기간이라 실사용 트래픽 데이터가 없다 — 이 문서의
모든 근거는 **코드 file:line**이며, 사용량/분석 데이터는 근거로 쓰지
않았다. 3개 감사팀(첫진입~홈 / 학습루프 / 보상·복귀)의 발견을 종합해
재편했다. file:line은 작성 시점에 재확인했으나, 이후 리팩터링으로 줄번호가
드리프트할 수 있다 — 함수/변수명으로 재검색해 확인할 것._

---

## 1. 요약 — 이 앱의 학습 경험 진단 (한 페이지)

**핵심 명제: 성실한 학생일수록 아무것도 자라지 않는 구조다.**

- 진도 바·유닛 완주·모자·마을·정원·도서관이 전부 `cleared` 배열 하나에
  의존하는데, 이 배열이 늘어나는 유일한 코드 경로는 홈 화면의 "퀴즈"
  버튼에서 하는 미니 퀴즈(`QuizGame`)를 틀렸을 때 생기는 레벨업 미션을
  3번 연속 맞히는 것뿐이다(`src/hooks/useStudent.js:929`). 매일의
  기본 학습 흐름인 `GuidedSession`(3분 데일리 리추얼)은 이 미션을
  의도적으로 만들지 않는다(`src/components/GuidedSession.jsx:26-29`).
  즉 "히어로 CTA(오늘의 학습 시작)"만 성실히 누르는 아이는 단어를 다
  풀어도 진도 0/60, 유닛 완주 불가다.
- 인출 연습(retrieval practice)이 기본 설정(쓰기 시험 꺼짐,
  `src/App.jsx:327-331`)에서 사실상 1회뿐이고 그마저 재인(recognition)
  이다 — 뜻을 먼저 보여준 뒤 바로 객관식 퀴즈를 푸는 구조라 회상(recall)
  이 0이다. 틀리지 않은 단어의 자동 재노출 경로는 코드 전체에 0개다.
- 보상 구조가 학습 목표와 정반대다 — 미니게임은 정답 1개당 별 10개를
  무제한 반복으로 받을 수 있는데(`src/utils/matchGame.js:5-6`), 정작
  진짜 단어 퀴즈(`QuizGame`)의 객관식 정답 자체는 별 0개다
  (`src/hooks/useStudent.js:1206-1212`, 별은 그 뒤 발음 단계에서만
  1개 지급).

이 문서의 개선 원칙은 **`cleared`의 정의를 절대 바꾸지 않는 것**이다.
모자/마을/박물관/책장이 전부 이 값을 읽고 있어 정의를 바꾸면 전부 다시
검증해야 한다 — 대신 진도 표시와 완주 판정이 읽는 **파생 집합**만 넓히는
쪽으로 개선한다(2장 참고).

---

## 2. 근본 원인 — `cleared` 단일 카운터

### 2.1 `cleared`가 늘어나는 유일한 지점

```
src/hooks/useStudent.js:917-930  answerMission()
  willClear = mission이 있고, 아직 done이 아니고, correctCount+1 >= 3
  ...
  if (willClear) {
    patch(prev => ({ cleared: [...prev.cleared, wordId] }))   // :929
    grantReward(3, `mission-clear:${wordId}`)
  }
```

이 `mission`은 `addMission(wordId)`(`useStudent.js:896`)로 생성되는데,
호출부는 코드 전체에서 딱 하나 — 홈 화면 퀴즈(`QuizGame`)에서 오답을
냈을 때다(`src/components/QuizGame.jsx:323`, `src/App.jsx:650`
`onAddMission={studentData.addMission}`).

`GuidedSession`(매일 기본 학습 흐름)은 헤더 주석에서 이를 명시적으로
밝힌다:

```
src/components/GuidedSession.jsx:26-29
// 오답 재시도: 본 코스에서 퀴즈를 틀린 단어는 그 세션 끝에 정확히 1회씩
// 퀴즈만(mode="quiz") 다시 풀린다. 재시도에서 또 틀려도 다시 큐에 넣지
// 않고, addMission(레벨업 미션 — 장기 복습 시스템)도 여기서 부르지
// 않는다(그 시스템은 별도 유지 — 운영자 승인 결정).
```

즉 **GuidedSession으로만 학습하는 학생은 `cleared`가 영원히 늘지 않는다.**

### 2.2 `cleared`가 구동하는 것 전부

| 기능 | 코드 위치 | 무엇을 읽는가 |
|---|---|---|
| 단어 목록 진도 바 | `src/components/WordBrowser.jsx:131-142` | `cleared.length / words.length` |
| 유닛 완주 판정 | `src/utils/attachment/attachmentCore.js:145-153` `completedUnits()` | 유닛의 모든 단어가 `clearedSet`에 포함되는지 |
| 탐험가/왕관 모자 | `src/utils/attachment/hatSystem.js:54,89` | `stats.clearedCount >= threshold` |
| 마을·정원 성장 단계 | `src/utils/attachment/worldProgress.js:13,31,57` | `growthPoints = stats.clearedCount` |
| 홈 밴드 "꽃 N송이" | `src/utils/attachment/paulTown.js:152-166` | (`cleared`는 아니고 `masteredCount` — 2.3 참고, 서로 다른 지표라 별도 표기) |

(홈 밴드 꽃 카운트는 `cleared`가 아니라 `masteredCount`를 쓰는데, 이
값은 코드 전체에서 절대 늘지 않는 별도 버그다 — 5장 A7번 참고.)

### 2.3 귀결

히어로 CTA(GuidedSession)만 쓰는 아이는:
- `WordBrowser`를 열어도 진도 0/N (`cleared`가 안 늘어서)
- 유닛을 아무리 다 풀어도 "완주" 배지 없음(`completedUnits`가 항상 빈 배열)
- 모자 갱신 없음, 마을·정원 성장 없음
- 복습 배너(레벨업 미션 배너, `Dashboard.jsx:181-186`)도 안 뜸 — 미션
  자체가 안 생기므로

### 2.4 개선 원칙

**`cleared`의 정의(레벨업 미션 3연속 정답 클리어)는 절대 바꾸지 않는다**
— 모자·마을·박물관·책장이 전부 이 값을 신뢰해 이미 지급된 보상의 의미가
소급 변경되면 안 된다. 대신:
- 진도 바/완주 판정이 읽는 집합을 `cleared ∪ {GuidedSession에서 실제
  학습 완료한 단어}`로 넓히는 **파생 집합**을 새로 만든다(1단계에서는
  손대지 않고, 3단계에서 설계).
- 또는 GuidedSession 오답도 `addMission`을 호출하도록 배선한다(2단계,
  6장 참고) — 이러면 `cleared` 정의 자체는 그대로 두고 그 정의가
  "채워지는 경로"만 넓어진다.

---

## 3. 화면별 감사 (첫 진입 → 유닛 완주 순서)

10개 렌즈: 불필요한 탭 / 혼란 문구 / 동기 하락 / 기억과학 / 간격 효과 /
인출 연습 / 발음 / 즉시 피드백 / 게임 설계 / 아동 심리. 해당 없으면
"해당 없음"으로 표기.

### 3.1 부팅·로딩

- **아동 심리 / 즉시 피드백** — 앱의 전역 첫 사용자 제스처(터치/클릭)
  하나에 마이크 권한 요청 + AudioContext 잠금 해제가 동시에 걸린다.
  `src/App.jsx:846-859`:
  ```
  // Unlock AudioContext + warm up speechSynthesis + ask for microphone
  // permission, all on the very first user gesture in the whole app
  const handler = () => { unlockAudio(); primeSpeech(); getMicStream().catch(() => {}) }
  document.addEventListener('touchstart', handler, { once: true, passive: true })
  document.addEventListener('pointerdown', handler, { once: true, passive: true })
  ```
  **왜 문제**: 이 리스너는 스크린과 무관하게 앱 전체에 걸려 있어, 처음
  보는 학생이 로그인 화면(이름 입력창 등)을 첫 탭하는 순간 브라우저의
  "마이크 권한을 요청합니다" 팝업이 뜰 수 있다. 앱이 뭘 하는지도 모르는
  아이에게 낯선 시스템 팝업이 먼저 뜬다.
  **최소 개선안**: 마이크 프리워밍을 로그인 완료 이후(첫 학습 화면
  진입) 첫 제스처로 늦춘다.
  **임팩트 3 / 난이도 S / 리스크 저** — 운영자 승인 불필요(순수 타이밍
  조정, 기능 변경 없음).

- 나머지 렌즈: 해당 없음(정적 로딩 단계).

### 3.2 로그인(StudentSelect)

- **혼란 문구 / 데이터 무결성** — `src/components/StudentSelect.jsx:44`:
  ```
  not_found: '해당 이름의 학생을 찾을 수 없어요. 처음이면 아래 "처음이에요" 탭에서 등록해주세요.',
  ```
  **왜 문제**: 이름을 잘못 입력했거나(오타) PIN을 깜빡한 학생도 같은
  메시지를 보고 "처음이에요" 탭으로 유도된다 — 결과적으로 동명이인이
  아니라 **같은 학생의 중복 계정**이 생성될 수 있다(규칙 4 — UUID
  식별은 지켜지지만, 신규 UUID가 하나 더 생겨 진도가 두 계정으로
  쪼개짐).
  **최소 개선안**: 문구를 "이름을 다시 확인해보세요. 그래도 안 되면
  선생님께 문의하거나 새로 등록하세요"로 완화, "새로 등록" 유도를
  후순위로.
  **임팩트 4 / 난이도 S / 리스크 저** — 문구만 수정, 운영자 승인 불필요.

- **동기 하락** — `StudentSelect.jsx:220-253`, 로그인 탭이 기본
  선택이고 등록/PIN 만들기는 별도 탭 — 구조 자체는 무난.

- 그 외 렌즈: 해당 없음.

### 3.3 첫 홈(빈 상태) / 3.4 홈 대시보드

- **불필요한 탭 / 인지 부하** — 기본 6버튼 + 애착 시스템 플래그 4종
  (모자/박물관/성장앨범/정원)까지 전부 켜져 있으면 "🧭 더 많은 메뉴"
  아코디언 안에 총 10개 내비게이션 타일이 접혀 들어간다
  (`src/components/Dashboard.jsx:605-643`, `<details>` 태그라 기본
  닫힘). 첫 로그인한 아이는 이 10개 기능의 존재를 전혀 모른 채 시작한다.
  **왜 문제**: 히어로 CTA에 집중시키려는 의도(주석에 명시)는 맞지만,
  다이어리/모자/정원처럼 "재미" 요소가 전부 숨겨져 첫날 동기부여 노출이
  0이다.
  **최소 개선안**: 최초 N회 로그인에 한해 아코디언을 기본 열림으로.
  **임팩트 3 / 난이도 S / 리스크 저** — 순수 표시, 운영자 승인 불필요.

- **거짓 정보 없음 확인** — `classWords.length > 0 ? ... : '단어 준비
  중'`(`Dashboard.jsx:614`) 처럼 실측값을 쓰고 있어 하드코딩 거짓말은
  이미 제거된 상태(과거 세션에서 수정됨).

- **동기 하락 / 성장 미반영** — 홈 밴드 "꽃 N송이"가 `masteredCount`를
  쓰는데(`src/utils/attachment/paulTown.js:157,166`) 이 값은 아래
  5장 A7에서 보듯 영구 0이다. 반면 "나의 정원" 화면은 `clearedCount`
  기반으로 자라난다(`worldProgress.js:31`) — **같은 세션 안에서 홈과
  정원이 서로 다른 성장 지표를 말한다.**
  **임팩트 5 / 난이도 M / 리스크 중** — 6장 참고.

- **레벨업 미션 배너** — `RecommendationBanner`
  (`Dashboard.jsx:181-186`)는 `activeMissions.length > 0`일 때만
  "복습할 단어가 있어요!"를 보여준다. `activeMissions`는 `cleared`와
  같은 미션 시스템(`addMission`)에서 나오므로, GuidedSession만 쓰는
  학생에겐 이 배너 자체가 절대 안 뜬다(2장과 동일 원인).

- **오늘의 미션 4칸** — `Dashboard.jsx:506-523`:
  ```
  <MissionBar label="단어 5개 보기" ... goal={GOAL} />
  <MissionBar label="예문 5번 듣기" ... goal={GOAL} />
  <MissionBar label="퀴즈 5개 풀기" ... goal={GOAL} />
  <MissionBar label="발음 5개 성공하기" ... goal={GOAL} />
  ```
  라벨에 텍스트로 "무엇을 하면 되는지"는 있지만 각 막대에 `onClick`이
  없어(코드상 어떤 `MissionBar` 인스턴스에도 탭 핸들러 없음) 탭해서
  바로 그 활동으로 이동하는 CTA가 아니다. "예문 5번 듣기"가 정확히 어느
  화면에서 채워지는지는 텍스트만으로 유추해야 한다.
  **임팩트 2 / 난이도 S / 리스크 저**.

### 3.5 GuidedSession (3분 데일리 리추얼, 본 학습 흐름)

- **기억과학 — 학습 기록이 미션에 반영 안 됨** — 2장 참고, 이 화면
  자체가 근본 원인의 발생지.
- **간격 효과 — 완료 카드의 "내일 복습" 안내가 오늘 오답을 안 셈**
  ```
  src/components/GuidedSession.jsx:245-249
  const tomorrowLine = todayPlanted
    ? '내일 아침, 새싹이 나 있을 거예요 🌱'
    : reviewQueueCount > 0
      ? `내일 복습할 단어 ${reviewQueueCount}개가 기다리고 있어요`
      : '내일도 3분이면 충분해요'
  ```
  `reviewQueueCount`는 `src/App.jsx:583` `reviewQueueCount={
  spellingReviewQueue.length}` — 이는 **어제 이전에** 넘어온 영구
  복습 대기열일 뿐, 오늘 쓰기 시험에서 막 틀린 단어
  (`spellingWrongToday`)는 자정 롤오버 전까지 여기 안 들어간다
  (`src/hooks/useStudent.js:759` — 리셋 시점에만 이월). 즉 오늘 5개
  틀려도 완료 카드는 "내일도 3분이면 충분해요"라고 말할 수 있다.
  **임팩트 3 / 난이도 S / 리스크 저** — `spellingWrongToday.length`를
  합산해 표시하면 됨.
- **게임 설계 — 완료 카드가 "그만하기"를 시각적 주 버튼으로 배치**
  ```
  src/components/GuidedSession.jsx:276-284
  <button ...gradient... onClick={onDone}>🏠 오늘은 여기까지</button>
  {!allDone && (
    <button ...outline... onClick={startNextSession}>▶ 다음 세션 계속하기</button>
  )}
  ```
  "오늘은 여기까지"가 위쪽 + 채워진(filled) 스타일, "다음 세션
  계속하기"가 아래쪽 + 테두리만(outline) 스타일 — 계속하길 원하는
  게 자연스러운 순간에 시각적으로는 그만두기가 더 눈에 띈다.
  **임팩트 2 / 난이도 S / 리스크 저**.
- **간격 효과 — 유일한 복습 진입점이 조건부로만 노출** —
  `onGoReview`/`reviewQueueCount`는 홈 대시보드가 아니라 이
  GuidedSession 완료 카드에만 배선돼 있고(`App.jsx:583-584`), 그마저
  `allDone && reviewQueueCount > 0`일 때만 버튼이 보인다
  (`GuidedSession.jsx:289-294`). 홈 화면(`Dashboard.jsx`) 자체에는
  `onGo('spellingReview')` 호출이 0건이다 — 유일한 다른 경로는 선물
  상자를 닫는 순간의 자동 강제 이동뿐이다(`App.jsx:202-205`). 즉 복습
  대기열은 "학생이 스스로 찾아가는" 진입점이 없고, 항상 다른 이벤트에
  얹혀 우연히 나타난다.
  **임팩트 5 / 난이도 M / 리스크 저** — 6장 참고.

### 3.6 WordDetail — 발음(PronounceStep)

- **인지 부하 / 아동 심리** — 탭 가능한 요소 실측 7개: 뒤로가기
  (`WordDetail.jsx:764`), 카드 전체 탭-재생(`:306-311`), 따라 말하기
  녹음 버튼(`:339-353`), 모르겠어요(`:359`), 다시 공부(`:366`),
  알아요(`:371`), 결과 후 "계속" 버튼(`:390-395`) — 여기에 정보
  블록(마스코트/단어/IPA/뜻박스/암기꿀팁) 5개가 더해진다. 8세 학생
  기준 한 화면의 인지 부하 상한을 넘는다.
  **임팩트 2 / 난이도 M / 리스크 중**(레이아웃 변경 필요).
- **즉시 피드백 — 정직성 차이(강점, 8장에도 기록)** — STT 제공자가
  설정 안 된 현재 상태에선 성공 문구가 "녹음 완료! 내 발음을 들어보고
  원어민과 비교해봐요"로, 발음이 맞았다고 단정하지 않는다
  (`WordDetail.jsx:173,178`).
- **발음 — 무음도 성공 판정** — `recordWithAutoStop`은 `minMs:2000,
  silenceMs:1000`(`WordDetail.jsx:135`)으로 항상 최소 2초를 녹음하고,
  `MediaRecorder.ondataavailable`은 무음이어도 0바이트가 아닌 청크를
  만든다(`src/utils/speech.js:581`). STT가 없으면 `blob.size===0`
  체크(`:154`)만 통과하면 바로 성공 처리된다 — 실질적으로 마이크
  권한을 주고 2초를 버티기만 해도 통과.
  **임팩트 4 / 난이도 L / 리스크 고**(STT 신규 도입 필요, 7장 제약의
  "비용 드는 API는 무료 대안 우선" 원칙과 충돌 검토 필요) — 3단계로
  분류.
- **회피 경로 — "알아요" 1탭이 전체 스킵** —
  ```
  src/components/WordDetail.jsx:282-285 (주석)
  // "알아요"는 상태 저장 + 이번 세션에서 바로 다음 단어로 진행
  // (onSkip = 부모의 onNext를 그대로 호출 — 이 단어의 나머지 단계는
  // 건너뜀
  :371  onClick={() => { onWordKnown?.(word.dbId); onSkip?.() }}
  :797  onSkip={() => (onNext ? onNext() : onBack?.())}
  ```
  한 번의 탭으로 예문·퀴즈·쓰기 단계가 전부 스킵되고, `wordStatus`가
  서버에 `'known'`으로 영구 기록된다(`useStudent.js:1329-1333`,
  `setWordStatus` API 호출). 발음이 부끄러운 아이가 이 버튼을 "회피
  수단"으로 습관화하면 그 단어는 다시는 예문/퀴즈에 노출되지 않는다.
  **임팩트 5 / 난이도 M / 리스크 고**(학습 흐름 변경, 운영자 승인
  필요) — 6장.

### 3.7 예문(ExampleStep)

- **회피 경로 0** — `src/components/WordDetail.jsx:401-455`에는
  "모르겠어요"/스킵 버튼이 없다. 유일한 진행 조건은
  `canProceed`(`:402`)이고, 이는 `SpeechBtn`의 `onAnyResult`
  (`:432`)로만 true가 된다. `onAnyResult`는 마이크 권한이 없거나
  실패해도 호출된다(`WordDetail.jsx:181-188`,
  `finish('fail', ..., ) ; onAnyResult?.()`) — 즉 **권한 거부·인앱
  브라우저 문제는 자동으로 통과되지만, 권한을 준 뒤 녹음 버튼 자체를
  아예 안 누르는 학생은 이 화면에 갇힌다**(어떤 버튼도 이 상태를
  건너뛰게 해주지 않음).
  **임팩트 3 / 난이도 S / 리스크 저** — "나중에 할게요" 류의 명시적
  스킵 버튼 추가.
- 그 외 렌즈: PronounceStep과 동일 패턴(중복 생략).

### 3.8 퀴즈(WordDetail 내 4지선다)

- **인출 연습 — 재인만, 회상 0** — 뜻이 방금 전 PronounceStep에서
  1분 이내 노출된 직후 같은 화면 세트 안에서 4지선다가 나온다. 문제
  자체가 순수 재인(recognition) — 회상(recall) 요소가 전혀 없다.
- 그 외: 해당 없음(구현 자체는 표준적인 4지선다).

### 3.9 SpellingQuestion (쓰기 시험, 기본값 꺼짐)

- **인지 부하 — 3회 오답 시점이 가장 복잡한 화면** —
  `wrongCount===3`일 때: 진행 바(`:304-315`) + 콤보 배지(선택,
  `:317-323`) + 방금 잠금 해제된 "🔊 탭하면 발음을 들려줘요" 인터랙티브
  버튼(`speakerUnlocked = wrongCount >= UNLOCK_AT(3)`,
  `:250,325-331`) + 힌트 버튼(`:338-346`) + 오답 리액션 이미지+문구
  (`:350-352`) + "내가 쓴 것" 표시(`:353-355`) + 입력창 + 확인 버튼
  (`:356-364`) — 총 8개 요소가 동시에 뜬다. 4번째 오답(reveal
  단계, `:368-389`)에서는 오히려 정답 공개로 화면이 단순해진다
  (`WRONG_MSGS` 정의는 `:34-38`, `next>=4` 분기는 `:237-246`).
  **왜 문제**: 학생이 가장 힘든 순간(3연속 오답)에 오히려 가장 많은
  걸 처리해야 한다.
  **임팩트 3 / 난이도 M / 리스크 저**.
- **동기 하락 — reveal 루프에 갇힐 수 있음** — `submitAnswer()`의
  `next >= 4` 분기(`SpellingQuestion.jsx:237-240`)는 4번째 이후
  오답마다 계속 `phase='reveal'`로 재진입시킨다 — 정답을 보고 그대로
  입력해도 오타를 내면 다시 reveal, 다시 reveal(탈출 버튼 없음).
  **임팩트 2 / 난이도 S / 리스크 저** — N회 이상 시 자동 통과 안전망
  추가.
- **즉시 피드백(강점)** — 1~2번째 오답은 정답/발음 모두 비공개로 유지
  해 "찍기"를 방지한다(`WRONG_MSGS`, `UNLOCK_AT=3`).

### 3.10 QuizGame (홈 퀴즈)

- **보상 역설 — 정답이 별 0개** — `onQuizAnswer(word.id, correct)`
  (`QuizGame.jsx:307`)가 흘러가는 `recordQuizAnswer`
  (`useStudent.js:1206-1212`)는 순수 분석용 카운터만 갱신하고
  `grantReward`/`addStars` 호출이 전혀 없다. 이 화면에서 별을 주는
  유일한 순간은 정답 후 이어지는 발음 단계 성공 시 1개
  (`QuizGame.jsx:147-148`, `grantReward` 경유 — 주석 `:333-335`).
  **임팩트 5 / 난이도 M / 리스크 중** — 6장.
- **발음 — 무음이 "발음 성공!" 단정** —
  ```
  QuizGame.jsx:127  recordWithAutoStop(stream, { maxMs: 5000, minMs: 2000, silenceMs: 1000, ... })
  QuizGame.jsx:145-148
    if (blob.size > 0) {
      setPhase('success')
      setMsg('발음 성공! ⭐ 1개 획득!')
      onSuccess()
    }
  ```
  STT 호출이 전혀 없이 `blob.size > 0`만으로 성공 처리한다. WordDetail의
  같은 상황 문구("녹음 완료!")와 달리 여기는 **"발음 성공!"으로 단정**
  한다 — 정직성 기준이 화면마다 다르다.
  **임팩트 4 / 난이도 S / 리스크 저**(문구를 WordDetail과 통일하는
  것만으로도 개선, STT 없이 즉시 가능).
- **레벨업 미션 트리거(강점 아님, 근본 원인의 발생지)** —
  `onAddMission(current.word.id)`가 오답 시 호출되는 유일한 지점
  (`QuizGame.jsx:323`).

### 3.11 LevelUpMission

- **기억과학(강점)** — 목록에서 뜻을 숨겨 재시도가 진짜 인출 연습이
  되게 설계돼 있다(8장).
- **보상 — 이미 아는 정보로 3연속 정답** — 미션 자체가 "방금 틀린
  단어"를 다시 묻는 구조라 학습 직후 재도전이라 난이도가 낮다. 별도
  간격 조건이 없다(3단계 대상).

### 3.12 SpellingReview

- **간격 효과(강점)** — `spellingWrongToday`가 자정을 넘겨
  `spellingReviewQueue`로 이월되는 유일하게 실제로 작동하는 간격
  복습 경로다(`useStudent.js:759`).
- **동기 하락 — 진입 경로가 항상 부수적** — 3.5에서 확인한 대로 홈에
  직접 진입 버튼이 없다.

### 3.13 WordBrowser

- **거짓 정보 없음 확인 / 강점** — 유닛 전환 직후 `cleared`가 새
  단어 목록보다 커져 분자>분모가 되는 걸 막기 위해 표시만 클램프한 것이
  이미 반영돼 있다(`WordBrowser.jsx:131-142`, `Math.min(100, ...)`).
- **이어하기 인덱스 축 불일치 → 되감김** —
  ```
  src/App.jsx:452-460  handleWordSelect(w)
    // v1.5: ... 지금 활성화된 학습 범위(sessionWords — 전체일 땐
    // classWords와 동일)를 기준으로 인덱싱한다.
    const idx = sessionWords.findIndex(cw => cw.id === w.id)   // sessionWords(필터 적용됨) 기준
    studentData.setLastWordIndex(safeIdx, currentUnitId)        // 이 축 그대로 저장
  src/App.jsx:465-472  goToWordIndex(idx)
    // 항상 반 전체 단어 기준(scope와 무관) — "이어서 학습하기"는
    // 필터링과 상관없이 지난번에 멈춘 그 자리부터 이어가는 게 맞는
    // 의미라서.
    setWord(classWords[idx])                                    // classWords(필터 무시) 기준으로 읽음
  ```
  **왜 문제**: `WordBrowser`에서 스코프 필터(예: "모르는 단어만")를
  켠 채 특정 단어를 탭하면, 저장되는 인덱스는 **필터된 목록 기준**
  인데, 다음 로그인 시 "이어서 학습하기"는 그 값을 **전체 목록 기준**
  으로 읽는다. 필터링된 단어가 전체 목록에서 더 앞쪽에 있으면 학생은
  이미 지나간 단어로 되감긴다. (참고: `App.jsx:349-351` 주석에서 보듯
  `mixedDirections` 배정에서는 이미 한 번 같은 클래스의 축 불일치를
  발견해 고친 이력이 있다 — 이번 건은 그 수정이 다루지 않은 별도
  지점.)
  **임팩트 3 / 난이도 M / 리스크 중**(인덱스 저장 로직 변경).

### 3.14 유닛 완주 시점

- **게임 설계 — 완주 순간 무표시** — `completedUnits`
  (`attachment.unitsDone`)는 `Dashboard.jsx:248`에서 Paul 인사말의
  맥락 데이터로만 쓰이고, 유닛을 다 풀자마자 뜨는 축하 화면이나
  "다음 유닛으로" CTA가 없다. `GuidedSession`의 완료 카드
  (`:250-299`)에도 유닛 완주를 알리는 문구가 없다 — "오늘 단어 전부
  완료"(그날 배정분 기준)와 "유닛 전체 완주"가 구분되지 않는다.
  **임팩트 3 / 난이도 M / 리스크 저**.

---

## 4. 학습과학 실측 (표)

### 4.1 단어 1개당 인출 횟수·종류

| 설정 | 인출 횟수 | 종류 | 비고 |
|---|---|---|---|
| 쓰기 시험 꺼짐(기본값, `App.jsx:327-331` `spellingTestEnabled: false`) | 1회 | 재인 100% / 회상 0% | 뜻이 PronounceStep에서 상시 노출된 직후(`WordDetail.jsx:335`) 바로 4지선다 퀴즈 |
| 쓰기 시험 켜짐(반별 관리자 설정) | 2회 | 재인 50% / 회상 50% | +SpellingQuestion(철자 production) |

기본 설정에서의 유일한 재인 1회조차, 뜻이 방금(1분 이내) 노출된 직후라
장기기억 인출이 아니라 **단기 시각기억 확인**에 가깝다.

### 4.2 한 세션 안 같은 단어 반복

집중 학습(massed practice) — 간격 0의 연속 3~4단계(발음→예문→퀴즈[→쓰기]),
전부 한 화면 전환 안에서 이어진다. 분산 요소는 GuidedSession 세션 끝의
오답 재시도 1회뿐(`GuidedSession.jsx:26-29`, 간격 없이 "그 세션 끝"에).

### 4.3 유닛 완주 후 재노출 경로 전수

| 경로 | 실제 작동 여부 | 근거 |
|---|---|---|
| ① 쓰기 오답의 익일 큐 | 작동(유일한 진짜 하루 간격) | `useStudent.js:759` |
| ② 홈 퀴즈 오답 → 레벨업 미션 | 작동하지만 간격 조건 없음(즉시 재시도 가능) | `useStudent.js:896-930` |
| ③ 교사 재배정 | 사실상 거의 안 일어남(플래너가 최근 배정 단어를 회피하도록 설계) | 배정 스케줄러 설계(코드 밖 운영 로직, 이 문서 범위 밖) |
| ④ 학생 자발적 탐색(WordBrowser 재방문) | 학생 의지에 전적으로 의존 | — |

**한 번도 틀리지 않은 단어의 자동 재노출 경로는 0개다** — ①②는 전부
"틀린 단어"만 대상으로 하는 오답노트류 시스템이다.

### 4.4 오답 시 처리 패턴

| 화면 | 즉시 정답 공개 | 재시도 | 나중에 다시 물음 |
|---|---|---|---|
| WordDetail 퀴즈(4지선다) | 즉시(오답 선택 시 정답 강조) | 그 자리에서 계속 | 아니오 |
| SpellingQuestion | 4번째 오답부터 | 예(3회까지 비공개 재시도) | 아니오(같은 세션 reveal 루프) |
| QuizGame | 즉시 | 아니오(오답 시 바로 미션행) | 레벨업 미션으로(간격 없음) |

공통 패턴: **"정답을 바로 알려주고 나중에 다시 묻지 않는다"** — 시험
효과(testing effect)의 핵심인 "지연 후 재시도"가 구조적으로 빠져 있다.

### 4.5 인지 부하

| 화면 | 탭 가능 요소 | 정보 블록 |
|---|---|---|
| PronounceStep | 7개(3.6절 실측) | 5개 |
| SpellingQuestion(3회 오답 시점) | 8개(3.9절 실측) | — |

8세 학생 기준 통상 권장 상한(4~6개 동시 조작 요소)을 두 화면 모두
초과한다.

### 4.6 부끄러운 아이의 녹음 회피 경로와 학습적 대가

| 단계 | 회피 경로 | 대가 |
|---|---|---|
| 발음(PronounceStep) | "알아요" 1탭 (`WordDetail.jsx:371`) | 예문·퀴즈·쓰기 전부 스킵 + `wordStatus='known'` 서버 영구 기록(`useStudent.js:1329-1333`) |
| 예문(ExampleStep) | 없음(`:401-455`에 스킵 버튼 부재) | 마이크를 아예 안 누르면 화면에 갇힘 |
| 마이크 권한 거부/인앱 브라우저 | 자동 통과 | `WordDetail.jsx:186-187` `onAnyResult?.()`가 실패 시에도 호출됨 |

**귀결**: 권한을 아예 안 주거나(인앱 브라우저 등) "알아요"로 도망치는
아이는 오히려 매끄럽게 통과하고, **권한을 준 뒤 그 화면에서 말하기
싫어서 버튼을 안 누르는 아이만 예문 단계에 갇힌다.**

---

## 5. 임팩트 순 마스터 랭킹

### S급 — 구조적 급소

1. **`cleared` 단일 의존으로 진도·완주·모자·마을 전부 정지** (임팩트 5)
   — 근거: 2장 전체. 개선안: 파생 집합 확장(6장 2단계). 난이도 M.
   리스크 중(학습 흐름에 영향 없는 표시/판정 로직만 바뀌지만 전
   화면에 영향). **운영자 승인 필요**(어떤 활동을 "진도"로 인정할지는
   제품 결정).

2. **GuidedSession 오답이 어디에도 남지 않음 → 복습·수집 동시 정지**
   (5) — 근거: `GuidedSession.jsx:26-29`. 개선안: 세션 종료 시
   `addMission` 호출 추가(1회 재시도에서도 틀린 단어만). 난이도 S~M.
   리스크 중(레벨업 미션 큐가 갑자기 커질 수 있어 UX 영향 검토 필요).
   **운영자 승인 필요**.

3. **보상이 학습과 역방향** (5) — 근거: 미니게임 10별/문항 무제한
   (`matchGame.js:5-6`, `MatchGameShell.jsx:30-32,92,151` — 세션ID가
   재시작마다 새로 발급돼 dedup을 우회) vs 퀴즈 정답 0별
   (`useStudent.js:1206-1212`). 예문은 재생 탭마다 카운트되고
   (`useStudent.js:960-962`, dedup 없음), 4/4 일일 미션 중 3개
   (예문/퀴즈/발음)는 dedup이 없어 한 단어 반복만으로 채울 수 있다
   (`useStudent.js:975-977` 주석이 이를 명시). 개선안: 미니게임 별
   상한 도입, 퀴즈 정답에도 소액 별 지급 검토. 난이도 M. 리스크 고
   (별 경제 전체 재조정). **운영자 승인 필요**.

4. **인출이 사실상 1회·재인만이고 재노출 일정 0** (5) — 근거: 4.1,
   4.3. 개선안: 3단계(회상 비중 확대는 운영자 결정). 난이도 L. 리스크
   고. **운영자 승인 필요**.

5. **유일한 간격 자산(spellingReviewQueue)이 홈에서 안 보임** (5) —
   근거: `Dashboard.jsx`에 `onGo('spellingReview')` 0건(직접 확인).
   진입은 `GuidedSession` 완료 카드의 조건부 버튼
   (`GuidedSession.jsx:289-294`, `allDone && reviewQueueCount>0`일
   때만)과 선물상자 닫기 후 자동 강제 이동(`App.jsx:202-205`)
   두 경로뿐 — 둘 다 "학생이 스스로 찾아가는" 진입점이 아니다. 개선안:
   홈 대시보드에 복습 대기열 배지/버튼 추가. 난이도 S. 리스크 저.
   **운영자 승인 필요 없음**(기존 데이터 읽기만 추가, 새 로직 없음).

6. **"알아요" 1탭이 예문·퀴즈·쓰기를 전부 삭제하고 서버에 영구 기록**
   (5) — 근거: `WordDetail.jsx:282-285,371,797`,
   `useStudent.js:1329-1333`. 개선안: "알아요"를 눌러도 예문/퀴즈는
   유지하고 발음 단계만 스킵하도록 축소, 또는 확인 다이얼로그 추가.
   난이도 M. 리스크 고(기존 v1.5 스펙 변경). **운영자 승인 필요**.

### A급 — 거짓말·버그 (즉시 수정 가능한 것 다수)

7. **`masteredCount` 영구 0 → 홈 "꽃 0송이" 고정 + 모자 1종 영구 잠금**
   — 근거: `masteredCount = wordStatus 값 중 'mastered'인 것의 개수`
   (`attachmentCore.js:54`)인데, 학생용 `wordStatus`에 `'mastered'`를
   쓰는 코드는 코드 전체에 0곳이다(`setWordKnownState`는 `'known'`/
   `'unknown'`만 씀, `useStudent.js:1335-1336`). `'mastered'`라는
   문자열이 실제로 대입되는 곳은 완전히 별개 기능인 문장 학습
   (`sentenceLearning.js`)의 `stage`뿐이라 이 값과 무관하다. 결과:
   홈 밴드 "꽃 N송이"(`paulTown.js:157,166`)가 항상 0이고, 마법사 모자
   (`hatSystem.js:75`, `masteredMustered` 임계값)도 영구 잠금.
   **정원 화면(`clearedCount` 기반)과 홈(`masteredCount` 기반)이 같은
   앱 안에서 서로 다른 성장 지표를 말한다.** 난이도 S(홈 밴드가
   `clearedCount`를 쓰도록 교체) ~ M(모자 임계값을 별도 지표로 재설계).
   리스크 저. **운영자 승인 불필요**(표시 버그 수정).

8. **로그인 실패 문구가 반대 탭으로 유도 → 중복 계정 생성** —
   `StudentSelect.jsx:44`. 3.2절. 난이도 S. 리스크 저. **승인 불필요**.

9. **1000⭐ 배지 `lion` 스티커 부재로 영구 소실** —
   `STAR_BADGES`(`useStudent.js:154-159`)의 마지막 항목
   `{ threshold: 1000, stickerId: 'lion' }`인데, 스티커 카탈로그
   (`src/data/stickers.js:16-56`)에 `id: 'lion'` 항목이 존재하지
   않는다(전수 확인). 1000별을 달성한 학생은 `stickers` 배열에
   `'lion'`이 추가되지만, 다이어리/도감 등 `STICKERS.find(s => s.id
  === 'lion')`로 조회하는 모든 화면에서 매칭 실패 — 빈 항목이거나
   렌더 자체가 스킵된다(개별 렌더 컴포넌트마다 다를 수 있어 정확한
   화면 증상은 컴포넌트별 재확인 필요). 난이도 S(카탈로그에 엔트리
   추가). 리스크 저. **승인 불필요**(버그 수정) — 단, 이미 `'lion'`을
   보유 중인 학생이 있다면 그 학생의 표시만 사후 복구됨(데이터 자체는
   이미 정상 저장돼 있어 별도 마이그레이션 불필요).

10. **스트릭 마일스톤이 high-water mark라 재획득 불가** —
    `milestoneStreak`는 "이미 축하한 최고 스트릭"(주석,
    `useStudent.js:235`)이고 `>` 비교로만 다음 마일스톤을 찾는다
    (`:1126` `m > milestoneStreak`). 스트릭이 끊겼다가 다시 쌓여도
    이미 넘었던 임계값(3/7/14/30일)은 다시 못 받는다 — 이건 규칙 7
    제약("스트릭 징벌/실제 손실 금지")과는 다른 문제(손실이 아니라
    "재획득 불가")라 임팩트는 중간이나 A급으로 분류. 난이도 M(재설계
    필요, 예: "이번 스트릭에서 도달한 최고치" 별도 축 추가). 리스크
    중. **운영자 승인 필요**(보상 재지급 정책 결정).

11. **무발화 2초가 "발음 성공! 별 1개"로 판정** — `QuizGame.jsx:
    127,145-148`, `recordWithAutoStop`의 `minMs:2000`
    (`speech.js:564-594`)과 STT 미호출 조합. WordDetail은 같은 상황을
    "녹음 완료!"로 정직하게 표기(`WordDetail.jsx:173,178`)하는데
    QuizGame만 "성공"을 단정. 난이도 S(문구만 통일). 리스크 저.
    **승인 불필요**.

12. **완료 카드 "내일 복습 N개"가 오늘 오답을 세지 않음** —
    `GuidedSession.jsx:245-249`, `spellingReviewQueue`만 세고
    `spellingWrongToday`(오늘 것, 자정 전엔 큐에 안 들어감,
    `useStudent.js:759`)는 미포함. 난이도 S. 리스크 저. **승인
    불필요**.

13. **이어하기 인덱스 축 불일치 → 되감김** — `App.jsx:452-460`
    (sessionWords 기준 저장) vs `App.jsx:465-472`(classWords 기준
    읽기). 3.13절. 난이도 M. 리스크 중. **승인 불필요**(버그
    수정이지만 인덱스 저장 스키마를 건드리므로 회귀 테스트 필요).

14. **`GOAL=5` 고정** — `useStudent.js:139` `const GOAL = 5`, 4개
    카테고리 판정(`:607-610`)이 전부 이 상수. 그날 배정 단어가
    5개 미만인 반/학생은 `wordsViewed`(5개 서로 다른 단어 필요,
    `:953-957` dedup)를 물리적으로 채울 수 없어 미션·선물·티켓·
    스트릭·"오늘 공부함" 히스토리 표시(`:1038-1039`
    `categoriesCompleted`)까지 전부 침묵한다. 난이도 M(배정 개수
    연동 또는 하한 완화 로직 필요). 리스크 중. **운영자 승인
    필요**(일일 목표 정책).

15. **`useAttachment` 판정이 로그인당 1회 → 성취 순간에 축하 없음,
    다음 로그인에 조용히 소급** —
    ```
    src/hooks/useAttachment.js:63-89
    // 판정 → 반영: 학생당·마운트당 1회 ...
    const ranRef = useRef(null)
    useEffect(() => {
      if (!studentId || !restoreChecked || ranRef.current === studentId) return
      ranRef.current = studentId
      ...
    }, [studentId, restoreChecked])
    ```
    이 훅은 `App.jsx:197`에서 최상위로 한 번만 호출되고, `App`
    컴포넌트는 화면 전환(`screen` state 변경)에 리마운트되지 않는다
    (화면 전환은 전부 조건부 렌더링, 컴포넌트 언마운트 아님). 즉
    `ranRef` 가드가 사실상 "로그인 세션당 1회"로 동작한다 — 코드
    주석(`:4-7` "Dashboard가 화면 전환 때마다 리마운트되므로 ... 자연히
    재판정된다")은 실제 동작과 어긋난다(주석이 실제보다 낙관적).
    결과: 세션 도중 모자/밀스톤 임계값을 막 넘겨도 그 순간엔 아무
    축하도 없고, 다음 로그인 때 조용히 지급된다. 난이도 M(재판정
    트리거를 활동 완료 콜백에 추가하거나 주기적 재평가 도입). 리스크
    중. **운영자 승인 필요**(어느 이벤트에서 재판정할지 설계 필요).

### B급 — 마찰·정서

16. **예문 단계 탈출구 0** — 3.7절, `WordDetail.jsx:401-455`.
    임팩트 3 / 난이도 S / 리스크 저.
17. **4회 오답 후 reveal 루프에 갇힘** — 3.9절,
    `SpellingQuestion.jsx:237-246`. 임팩트 2 / 난이도 S / 리스크 저.
18. **"모르겠어요"가 무반응** — `WordDetail.jsx:359`는 버튼 자체
    색상만 바뀔 뿐(`wordStatus==='unknown'` 스타일), 성공/실패 흐름에
    있는 HeroReaction·효과음 같은 리액션이 없다. 임팩트 2 / 난이도 S /
    리스크 저.
19. **첫날 재미 노출 0(접힌 메뉴 최대 10기능)** — 3.3절,
    `Dashboard.jsx:605-643`(기본 6 + 애착 시스템 플래그 4종 전부 켜진
    경우 최대 10개 — 프롬프트에서 언급된 "12개"는 실측과 다름, 실제
    타일 수는 최대 10개다). 임팩트 2 / 난이도 S / 리스크 저.
20. **마이크 권한이 로그인 첫 터치에 걸림** — 3.1절, `App.jsx:846-859`.
    임팩트 3 / 난이도 S / 리스크 저.
21. **완료 카드가 "그만하기"를 주 버튼으로** — 3.5절,
    `GuidedSession.jsx:276-284`. 임팩트 2 / 난이도 S / 리스크 저.
22. **3회 오답 보상(발음 듣기 잠금 해제)이 이미 아는 정보** —
    `SpellingQuestion.jsx:23,250` `UNLOCK_AT=3` — "보상"이 정답을
    맞히는 데 실질적 도움을 주는 힌트가 아니라 단순 발음 재생이라
    체감 보상감이 약함. 임팩트 1 / 난이도 M / 리스크 저.
23. **유닛 완주 무표시 + 다음 유닛 안내 없음** — 3.14절. 임팩트 3 /
    난이도 M / 리스크 저.
24. **선물 직후 예고 없이 시험(스펠링 리뷰)로 강제 이동** —
    `App.jsx:202-205` `handleDismissGift`가 선물 닫기 직후 조건부로
    바로 `spellingReview` 화면으로 전환 — 선물을 받은 좋은 기분
    직후 갑자기 오답 시험 화면이 뜬다. 임팩트 2 / 난이도 S / 리스크
    중(전환 자체는 의도된 기능이라 문구/전환 애니메이션 추가가 안전한
    선).
25. **미션 4칸이 어디서 하는지 안 알려줌** — 3.4절,
    `Dashboard.jsx:518-521`(라벨은 있으나 각 칸에 탭-이동 CTA 없음).
    임팩트 2 / 난이도 S / 리스크 저.

---

## 6. 실행 로드맵 (3단계)

### 1단계 "거짓말부터 없앤다"

대상: 화면이 사실과 다른 말을 하는 항목 — **A7, A8, A9, A11, A12,
A13**과 B18/B19/B20/B21/B25의 문구·CTA 보강. 대부분 S급 난이도, 저위험,
**운영자 승인 없이 착수 가능**(표시/문구 수정, 기존 데이터를 다른 방식
으로 "읽기"만 하는 수정, 새 학습 로직 없음). 예외: A10(스트릭
재획득 정책), A14(GOAL 정책)은 보상 지급 규칙 변경이라 운영자 결정
필요 — 1단계에 포함하되 착수 전 승인 표시를 남긴다.

**완료 조건**: `masteredCount`/`cleared`가 화면마다 일관되게 표시되고,
로그인 실패·"내일 복습"·발음 판정 문구가 실제 상태와 일치하며,
`lion` 스티커가 정상 렌더된다. `npm run build` + 관련 `verify:*` 통과.

### 2단계 "성장을 되살린다"

대상: **S1(파생 집합 확장), S2(GuidedSession 오답 미션 승격),
S5(홈에 복습 진입점)**. 진도/완주/보상이 실제 매일 쓰는 학습 흐름을
반영하도록 배선을 넓히는 단계 — `cleared`/미션 시스템의 정의는 그대로
두고 "채워지는 경로"만 늘린다. **학습 흐름 변경이라 전부 운영자 승인
필요.**

**완료 조건**: GuidedSession만 쓰는 학생도 진도 바가 실제로 늘고,
유닛 완주가 가능하며, 홈 화면에서 복습 대기열로 직접 이동하는 버튼이
상시 노출된다. 회귀 없음을 `npm run verify:all`로 확인.

### 3단계 "기억을 만든다"

대상: **S4(인출 회상 비중 확대 — 쓰기 기본값 on/off 재검토)**,
**S3(보상 재조정 — 미니게임 상한/퀴즈 정답 보상)**, 간격 반복 배선
(Leitner 계열 로직이 있다면 그 배선 확인 및 `review_data` 덮어쓰기
이슈 선행 조사·수정), **미션(레벨업) 클리어에 최소 간격 조건 추가**.
전부 학습 과학/보상 경제/비용(STT 등) 트레이드오프가 얽혀 있어 **전부
운영자 결정 사항**.

**완료 조건**: 신규 XP 발생원·곱연산 배수·무작위 보상·티켓 가챠 없이
(7장 제약 준수), 한 번도 틀리지 않은 단어도 최소 1개 이상의 자동
재노출 경로를 갖는다.

---

## 7. 지켜야 할 제약 (모든 제안이 준수)

- 새 XP 발생원 금지
- 곱연산 배수 금지
- 무작위 보상/신규 화폐 금지
- 티켓 가챠 금지
- 스트릭 징벌/실제 손실 금지
- 개인 순위 공개 금지
- 홈 화면 80% 불변
- 미션 4칸에 5번째 칸 추가 금지
- DB에는 사실만 기록(과장된 상태 문자열 금지)
- 학생 신규 기능은 운영자 승인 필요
- `cleared`의 정의(레벨업 미션 3연속 정답 클리어) 불변

---

## 8. 이 감사가 확인한 강점 (정직성)

- **`cleared` 기반 월드/모자와 별 경제가 의도적으로 분리된 설계** —
  모자/마을/정원이 `cleared`(학습 성실도 지표)를 읽고, 별/티켓은 완전히
  별도 경제(`grantReward`/`ticketLedger`)로 관리돼 "돈으로 성장을
  사는" 구조가 아니다(`hatSystem.js`, `worldProgress.js`,
  `useStudent.js` 티켓 경제 주석 전반).
- **`paulTown`의 "없는 데이터는 주장하지 않는다" 폴백** —
  `paulTown.js`의 홈 밴드/디스커버리 로직이 표본이 부족하면 관련 문구
  자체를 생성하지 않는 방어적 설계를 취한다(`:100` 등 조건부 `push`).
- **`GrowthAlbum`의 표본 부족 시 비교 생략** — 통계적으로 근거 없는
  비교 문구를 억지로 만들지 않는 설계(성장 앨범 관련 코드 전반).
- **쓰기 오답의 익일 이월 — 유일하게 실제로 작동하는 간격 복습**
  (`useStudent.js:759`, 4.3절 ①).
- **LevelUpMission 목록의 뜻 숨김 처리** — 재도전이 단순 재확인이
  아니라 진짜 인출 연습이 되도록 설계돼 있다.
- **SpellingQuestion의 오답 단계별 정보 통제** — 1~2번째 오답에서
  정답/발음을 모두 비공개로 유지해 성급한 정답 노출을 막는다
  (`WRONG_MSGS`, `UNLOCK_AT`).
- **WordDetail의 정직한 발음 판정 문구** — STT 미설정 상태에서
  "발음 성공"을 단정하지 않고 "녹음 완료"로 표기한다(QuizGame과 대비
  되는 지점이자, 이 문서가 QuizGame 쪽에 요구하는 개선의 참조 기준).
